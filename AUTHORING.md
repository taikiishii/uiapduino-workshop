# 新しい資料の作り方（オーサリングガイド）

このリポジトリの教材は **Markdown だけ書けば作れます**。HTML・QRコード・もくじ（index）は
`build.py` が自動生成するので、`docs/` の中を直接さわる必要はありません。

**1つの章 = 1つのフォルダ**です。フォルダの中に本文（index.md）と画像（image/index/）を入れます。
画像は VS Code で貼り付ければ自動で `image/index/` に保存されるので、フォルダは意識しなくてOKです。

```
content/                    ← ここだけ編集する
  _template/index.md        ← 新しい章のひな形
  i1_about/                 ← 章フォルダ（フォルダ名 = <id>_<slug>）
    index.md                ←   本文（Markdown）
    image/index/            ←   写真・画面キャプチャ（VS Codeが自動でここに保存）
templates/                  ← HTMLのひな形（基本さわらない）
build.py                    ← 変換スクリプト
docs/                       ← 自動生成される公開ファイル（さわらない）
  i1_about/{index.html, qr.svg, image/index/}   ← content と同じ形で出力される
  assets/{deck.css, deck.js, hakase.png, hakase-face.png, qr.svg}
  index.html
verify/                     ← 実機検証用スケッチ（教材ではない）
```

章フォルダ名の頭は **i=導入編 / b=基本編 / c=制御編 / e=発展編 / x=付録** ＋ 連番（例：`b3_pwm`, `e4_servo`, `x2_trouble`）。

---

## 手順（4ステップ）

1. **ひな形フォルダをコピー**して新しい章を作る
   ```bash
   cp -r content/_template content/e7_newsensor
   ```
2. `content/e7_newsensor/index.md` を**Markdownで書く**（下の「書き方」参照）
3. 写真・画面キャプチャを本文に貼り付ける（VS Codeなら自動で `content/e7_newsensor/image/index/` に保存）
4. **ビルドして確認 → 公開**
   ```bash
   python build.py                 # docs/ を再生成（HTML・QR・もくじ）
   # docs/e7_newsensor/index.html をブラウザで開いて確認
   git add -A && git commit -m "Add chapter e7" && git push
   ```
   push すると数十秒で GitHub Pages に反映されます。

> はじめて使うときだけ: `pip install segno pillow`（QR生成と画像縮小に使います）

---

## 書き方

### フロントマター（ファイル先頭の設定）

| 項目 | 説明 | 例 |
|---|---|---|
| `id` | 章の番号。ならび順・QR・画像フォルダに使う | `b3` / `e4` |
| `slug` | URLになる英語名 | `pwm` |
| `section` | もくじのどのセクションに入れるか | `導入編` / `基本編` / `制御編` / `発展編` / `付録` |
| `emoji` | もくじカードの絵文字 | `🌗` |
| `num` | カードの番号バッジ | `3` |
| `color` | カードの枠色 | `c1`〜`c5` |
| `nav_title` | 画面上部・タブに出るタイトル | `③ LEDの明るさを変えよう` |
| `card_title` | もくじカードの見出し | `LEDの明るさを変えよう` |
| `desc` | もくじカードの説明（1〜2文） | `ついている・消えているだけでなく…` |
| `wip` | もくじカードに「🚧 工事中」マークを付ける（作りかけの章） | `true` / `準備中` |

**書き上がったら `wip` の行を消す**と、もくじの「工事中」マークが外れます。

### スライドの記法（本文）

| 書き方 | 意味 |
|---|---|
| `---`（行頭・単独） | スライドの区切り |
| `{cover}` | 表紙レイアウト（中央寄せ）。QRは自動で右下に付きます |
| `# 見出し` | スライドのタイトル |
| `:::` | ここで左右2カラムに分ける（左=説明、右=画像 または コード） |
| `1. ` / `- ` | 番号つき手順／箇条書き |
| `**太字**` `` `コード` `` | 強調・コード表示 |
| `![キャプション](image/index/xxx.png)` | 画像（連続で書くと横並び） |
| `> ふつうの文` | 博士のヒント吹き出し（緑） |
| `> ⚠ 文` | 注意の吹き出し（オレンジ） |
| `> 💪 文` | チャレンジの吹き出し（青） |
| `>> 文` | 吹き出しを**全身の博士**に固定（見せ場用） |
| `>\| 文` | 吹き出しを**顔だけの博士**に固定 |
| ` ```cpp ` 〜 ` ``` ` | コードブロック（色分け・行番号・コピーボタン付き） |
| ` ``` ` 〜 ` ``` `（言語なし） | ただの枠つきテキスト。**長いURLを配るとき**に便利（コピーボタンが付く） |

> 博士のアイコンは、本文が60文字未満なら**顔だけ**、60文字以上なら**全身**に
> 自動で切り替わります。意図的に変えたいときだけ `>>` ／ `>|` を使ってください。

> ⚠ **表（`| … | … |`）は使えません。** スライドではそのまま文字として出てしまいます。
> 比べたいときは「**項目 … Aはこう。Bはこう**」の形の箇条書きにしてください。

**定番の並び**は「表紙 →『なにをするの？』→ 手順①②… 」です。`_template/index.md` がその形になっています。

### スマホで見たときの表示

パソコンでは 16:9 のスライドですが、**スマホなど幅780px以下の画面では自動で縦長のカードに変わります**。

- `:::` の左右2カラム → **上に説明・下に画像**の縦並び
- 連続して書いた画像（横並び）→ こちらも**縦に1枚ずつ大きく**

**発表モード**と**印刷 / PDF** はスマホでも 16:9 のスライドのままです。

---

## コードの見せ方

Arduino のプログラムは、**コードブロック**で書きます。この教材では言語は **`cpp`** を使います
（`ino` / `arduino` と書いてもOK。表示は「Arduino (C++)」になります）。

````markdown
```cpp はじめてのLチカ          ← 「言語」＋（省略可）「キャプション」
const int LED = 2;

void setup() {
  pinMode(LED, OUTPUT);
}
```
````

- 言語のうしろに書いた文字は、コードの上に**小見出し**として出ます
- 色分け・**行番号**は自動です
- `pinMode` `digitalWrite` `analogRead` などの Arduino の関数は**青く**表示されます

表示は4つのモードで自動的に切りかわります。

| モード | 見え方 |
|---|---|
| **Web（パソコン）** | 右上の「📋 コピー」で**ワンクリックコピー**（行番号は入りません） |
| **スマホ** | 文字を読めるサイズに拡大。長い行は**折り返して**表示 |
| **発表モード** | 行数に合わせて文字が自動で大きくなります。コピーボタンは**消えます** |
| **印刷 / PDF** | 背景と色分けはそのまま印刷。コピーボタンは**消えます** |

書くときのコツ：

- **1スライドのコードは 14行くらいまで**。長いと自動で文字が小さくなります（分けるのがおすすめ）
- **字下げは半角スペース2つ**（Arduino IDE の自動整形 `Ctrl+T` に合わせる）
- `:::` の右カラムをコードだけにすると、コード側が少し広く表示されます
- **`analogWrite` を使うときは `analogWriteResolution(8);` を `setup()` に入れる**
  （このボードの既定は 0〜4095。入れないと 0〜255 のつもりで書いたコードがほとんど光りません）

---

## 写真・画面キャプチャの渡し方

- いちばん簡単なのは **VS Code で画像をコピー → 本文に貼り付け**。自動で **`image/index/`** に保存され、
  `![](image/index/…)` の参照も入ります（`.vscode/settings.json` で保存先を設定済み）
- 手で置くときも、その章の **`image/index/`** に入れて `![説明](image/index/step1.png)` と書けばOK
- ビルド時に `docs/<章>/image/index/` へコピーされ、大きすぎる画像は自動で縮小されます
- 名前は順番がわかるように `01_wiring.png` `02_ide.png` … と付けると迷いません
- **配線の写真**は、ジャンパーの色と挿す位置がはっきり分かる角度で撮る。真上から撮るのが基本

---

## マーキング（矢印・囲み・「ここをクリック」）

画像に矢印や囲みを付けたいときは、**Greenshot**（Windows・無料）が手軽でおすすめです。

1. https://getgreenshot.org/ からダウンロードしてインストール
2. `PrintScreen` キーで範囲を選んでキャプチャ → そのまま注釈エディタが開く
3. ツールバーで **矢印・四角・ハイライト・番号・文字** を追加
4. 「名前を付けて保存」で `content/<章>/image/index/` に PNG 保存（または VS Code に貼り付け）

### マーキングのコツ（子ども向け）

- 押す場所・挿す穴は **赤い四角で囲む** ＋ 短い言葉（「①ここに挿す」）
- 1枚に矢印を入れすぎない（多くても2〜3個）
- 本文の「**番号つき手順**」＋写真で大半は伝わるので、マーキングは"ここぞ"だけに

---

## もくじのセクションについて

`section` に書いた名前でカードが自動的に振り分けられます。

- `導入編` … Arduino IDE の準備と、はじめてのLチカ
- `基本編` … LED・スイッチ・つまみ（デジタル/アナログの入出力）
- `制御編` … 変数・for・if・関数（プログラムの組み立て方）
- `発展編` … センサーとモーター、作品づくり
- `付録` … 部品リスト・ピン配置・トラブル対応・はんだ付け・ライブラリ・カラーコード

順番は `id` の昇順です。新しいセクションを増やしたいときは `build.py` の `SECTIONS` に追記してください
（もくじの区切り線の色は `templates/index.html` の `.s-◯◯ .section-line` で決めています）。
