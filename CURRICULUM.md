# UIAPduino 体験ワークショップ カリキュラム案（v0.2 ドラフト）

作成日: 2026-08-31 / 雛形リポジトリ: `../microbit` / 全33章（導入3・基本5・制御4・発展5・作品8・付録8）

---

## 0. 前提と想定

| 項目 | 内容 |
|---|---|
| 対象 | **小学校高学年〜中学生**（文字＝C++ を書くので、micro:bit 版より対象年齢は高め） |
| 人数 | 10〜20名（メンター 1人あたり 3〜4名） |
| 開催 | **まずは半日（3時間）**。同じ教材のまま、**進むセクションを増やして1日・複数日に拡張**（§4 コース設計） |
| ボード | UIAPduino Pro Micro CH32V003 V1.4（¥290／RISC-V 48MHz／Flash 16KB／RAM 2KB）※**ピンヘッダはんだ付け済みで配布** |
| PC | Windows 推奨（Arduino IDE 2.x）。参加者持ち込み or 貸出 |
| 教材の形 | micro:bit 版と同じ「1章＝1フォルダの Markdown → `build.py` → Webページ／発表スライド／印刷PDF」 |

> このカリキュラムは **micro:bit 版の構成（導入 → 基礎 → 拡張）を踏襲**しつつ、Arduino＝文字のプログラムであることに合わせて
> 「制御編」を1セクション独立させています。

### 設計方針：セクション積み上げ型

**導入編から順に積み上げ、「どこまで進むか」で開催期間を変える**構成です。各セクションの終わりが必ず
「動くもの1つ」で締まるので、どこで終わっても達成感が残ります。

```
半日(3h)   : 導入編 ────→ 基本編（前半）                     … Lチカ〜スイッチ・明るさ
1日(6h)    : 導入編 ────→ 基本編 ────→ 制御編                … 自分でくりかえし・条件が書ける
複数日     : 導入編 ────→ 基本編 ────→ 制御編 ────→ 発展編   … センサー → モーター → 作品づくり
```

---

## 1. 教材の全体像

セクションは4つ ＋ 付録。章フォルダ名は micro:bit 版と同じ `<id>_<slug>` 形式。

### 🔰 導入編（`i`）

| id | 章タイトル | 学ぶこと | 使う部品 | 目安 |
|---|---|---|---|---|
| `i1_about` | マイコンと UIAPduino ってなに？ | コンピュータとマイコンの違い／Arduino とは／290円のボードの話／基板の各部の名前とピンの見方 | ボードのみ | 15分 |
| `i2_setup` | Arduino IDE をじゅんびしよう | IDE のインストール／追加ボードマネージャURL／UIAPduino の追加／ボード選択 | PC・USBケーブル | 30分 |
| `i3_blink` | はじめてのLチカ | `setup()` と `loop()`／`pinMode` `digitalWrite` `delay`／**書き込み手順**／点滅の速さを変える | ボード単体（内蔵LED） | 30分 |

### 🔌 基本編（`b`）— 入出力

| id | 章タイトル | 学ぶこと | 使う部品 | 目安 |
|---|---|---|---|---|
| `b1_led` | ブレッドボードにLEDをつなごう | ブレッドボードのしくみ／LEDの向き（アノード・カソード）／なぜ抵抗が要るか／外部ピンへの出力 | LED・1kΩ・ブレッドボード | 25分 |
| `b2_switch` | スイッチでLEDをつけよう | `digitalRead`／`INPUT_PULLUP`（押すと LOW）／デジタル＝0か1 | タクトスイッチ | 25分 |
| `b3_pwm` | LEDの明るさを変えよう | `analogWrite`（PWM）／0〜255／「速い点滅で明るさに見える」しくみ | LED・1kΩ | 25分 |
| `b4_analog` | つまみで明るさを変えよう | `analogRead`（0〜1023）／アナログとデジタルの違い／`map()` で範囲を変換 | 可変抵抗 10kΩ | 30分 |
| `b5_buzzer` | 音を鳴らそう | `tone()` / `noTone()`／音の高さ＝周波数 | 圧電ブザー（**受動型**） | 20分 |

### 🔁 制御編（`c`）— プログラムのしくみ

| id | 章タイトル | 学ぶこと | 使う部品 | 目安 |
|---|---|---|---|---|
| `c1_variable` | 変数をつかおう | 変数とは／`int`／`const int` でピン番号に名前をつける／値を変えると動きが変わる | 前章の回路 | 20分 |
| `c2_for` | くりかえし（for） | `for` 文／カウンタ変数／なめらかフェード・流れるLED（ナイトライダー） | LED×3 | 30分 |
| `c3_if` | もし〜なら（if / else） | 条件分岐／比較演算子／しきい値判定／`else if` | 可変抵抗・LED | 30分 |
| `c4_function` | 配列と関数でまとめよう | 配列で複数LEDをまとめる／自作関数（`void blink(int pin)`）／同じ処理を1か所に | LED×3 | 25分 |

### 🚀 発展編（`e`）— まず「感じる」、つぎに「動かす」

**前半＝センサー編（入力の拡張）／後半＝モーター編（出力の拡張）** の順。基本編で `analogRead` をやった
直後の流れに乗るので、センサーから入るほうが接続がなめらかです。

| id | 章タイトル | 学ぶこと | 使う部品 | 目安 |
|---|---|---|---|---|
| `e1_light` | 光センサーで暗くなったら光る | CdS＋分圧回路／点滅の速さで値を見る／つまみでさかいめを決める | **CdS（暗抵抗1MΩ）**・可変抵抗10kΩ | 30分 |
| `e2_temp` | 温度をはかろう | アナログ温度センサー／電圧→温度の計算／校正／しきい値で警告 | **MCP9701-E/TO**（⚠ 9700 と別物） | 30分 |
| `e3_distance` | ものとの距離をはかろう | 超音波センサー／回数を数えて時間をはかる／時間から距離を求める | HC-SR04（グループ共用可） | 30分 |
| `e4_servo` | サーボモーターを動かそう | 角度指定（0〜180°）／パルス幅で角度が決まる仕組み／電源の考え方 | SG90 | 30分 |
| `e5_motor` | モータードライバでDCモーター | なぜドライバICが要るか／正転・逆転・速度（PWM）／別電源 | **DRV8835モジュール**（秋月 109848）+ DCモーター | 35分 |


> `e3_distance` + `e4_servo` で「近づくと動く」、`e1_light` + `e5_motor` で「暗くなったら回る」など、
> **センサー編とモーター編を掛け合わせた作例**が `e6_project` のネタになります。

### ⬜ 追加予定：人感センサー（`e2_motion`）— 実機まち

光センサーと温度センサーのあいだに入れる。**実機で確かめてから書く**（2026-09-21 に決定）。

- センサーは **SB412A**（秋月 109002・¥640）。micro:bit 版の教材と同じ部品。
- 入れると **`e2_temp` 以降を1つずつ繰り下げる**ことになる。
  表示順は frontmatter の `id` で決まる（kit の `build.py`：`items.sort(key=... get("id"))`）ので、
  フォルダ名・`id`・`num`・サンプルのファイル名・章をまたぐ参照をまとめて付け替える。
  `e2_temp`→`e3_temp` ／ `e3_distance`→`e4_distance` ／ `e4_servo`→`e5_servo` ／ `e5_motor`→`e6_motor`

**⚠ 先に決めないといけないこと**

SB412A の **出力の HIGH は 3V 固定**（電源電圧ではない）。このボードは 5V 動作なので、
`digitalRead` がそれを HIGH と判定するかが資料だけでは決まらない
（記録にあるのは VIH の**最大**だけで、しきい値としての**最小**は未記録。
WCH のデータシート本体は配布ページからも GitHub からも取得できなかった）。

確認用スケッチ `verify/v11_pir/` を用意した。結果しだいで章の形が変わる。

| 実測 | 章の書き方 |
|---|---|
| `digitalRead` でそのまま読めた | いちばん素直。デジタル入力の章になる |
| プルアップで 5V まで上がった（オープンコレクタだった） | 外付けプルアップを配線に入れる |
| どちらもだめ | `analogRead` としきい値で読む（直前の `e1_light` と同じ形） |

**参考**：micro:bit 版は同じセンサーを **3V** で使っているが、データシートの下限は **3.5V**。
あちらも見直しの余地がある（別リポジトリなので、こちらでは手をつけていない）。

### 🧩 拡張編（`m`）— ライブラリを使う部品（2026-09-24 新設）

発展編の次、作品編の前。**ライブラリを使うのはこの編だけ**（発展編までは使わない方針は変えない）。
フォルダ名の頭は **m**（module）。

| id | 章タイトル | 学ぶこと | 使う部品 | 目安 |
|---|---|---|---|---|
| `m1_lcd` | 文字を表示しよう（LCD） | ライブラリの入れかた／I2C の4本／文字と数の表示／つまみの値を数で読む | **LCD 1602＋I2C変換基板 FC-113**（PCF8574 系）| 30分（仮） |

- **ねらい**：このボードは**シリアルモニタが使えない**。LCD があれば**数をそのまま読める**。
  センサーの校正や作品のデバッグが楽になるので、作品編の前に置いた。
- **配線**：GND・VCC(5V)・SDA＝**3番**(PC1)・SCL＝**4番**(PC2)。基板に 2.2kΩ プルアップあり。
  3番・4番は**下の行**のピンなので、線は a・b 行から出す。
  ⚠ 3番は b2 のスイッチでも使う。**いっしょに使うときはスイッチを別のピンへ**（章に明記ずみ）。
- **変換基板（実物写真で確認）**：端子の列を上にすると、4本は上から **GND・VCC・SDA・SCL**
  （表から見て左はし・裏から見て右はし）。**青いつまみ＝コントラスト**、
  **「LED」の黒いジャンパー＝バックライト**。A0〜A2 のアドレス用パッドは未ハンダ。
- **アドレス**：手元の基板は **0x3F**（2026-09-24 実測）。章のコードは 0x3F。
- **ライブラリ**：「LiquidCrystal I2C」（Frank de Brabander）。✅ 実機で表示を確認。
  ⚠ architectures=avr なので警告が出る可能性。だめなら「LiquidCrystal_PCF8574」（Matthias Hertel・architectures=*）。
- ⚠ **LCD の文字表に ℃ は無い**。💪 の温度表示は「`C` と書く」と案内した（`°` は 0xDF で出せるが教材では扱わない）。
- ⚠ 数を上書きすると前の桁が残る（1023 → 5 で「5023」）。空白を出して消すことを章で教える。
- 作図は `tools/fig_bb_lcd.py`（`bb_lcd.png` と、つまみを足した `bb_lcd_vr.png`）。
- **実機確認まち**（`wip: 実機確認まち`）。§9 の「第4段階：拡張編」を参照。

### 📎 付録（`x`）

| id | 章タイトル | 内容 |
|---|---|---|
| `x1_parts` | 部品リストと回路の見方 | 部品の名前・記号・ブレッドボード配線図の読み方 |
| `x2_trouble` | 困ったときは | 書き込めない／LEDがつかない／IDEのエラー文の見方（§5 の内容をカード化） |
| `x3_pinout` | ピン配置早見表 | 公式ピン配置図（CC BY 4.0）／PWM・アナログ可否／I2C・SPI・UART／使ってはいけないピン |
| `x4_solder` | ピンヘッダをはんだ付けしよう | **自分でボードを買った人むけ**。道具／こての当て方／ピンヘッダの固定／はんだ付けの良い例・悪い例／やり直し方／安全上の注意 |
| `x5_library` | ライブラリを追加しよう | **本編では一切不要**。自由制作で部品を増やしたい人むけ。`#include` とは何か／ライブラリマネージャ／16KBに収まらないときの話 |
| `x6_resistor` | 抵抗のカラーコードの読み方 | 色の帯の意味／色と数字の対応／1kΩ と 10kΩ の読み方／まちがえやすい色。b1 から参照している |
| `x7_mentor` | メンター用 早見表 | **当日メンターが手元に置く1枚**。章ごとに使うピン／つまずきどころ Top5／切り分けの順番／時間の目安／事前準備／声のかけ方 |
| `x8_handout` | 配布用 1枚紙 | **当日くばる紙**。もくじのQRコードと書きこみ手順だけを1ページに。表紙なしの1スライド構成なので、印刷すると ちょうど1枚になる |

---

## 2. 各章の中身（詳細案）

各章のスライド構成は micro:bit 版と同じ「**表紙 → なにをするの？ → 手順①②③ → チャレンジ（💪）**」で統一します。

### i1 マイコンと UIAPduino ってなに？

- スマホもエアコンも中に小さなコンピュータ（マイコン）が入っている、という導入
- micro:bit との違い＝「画面もボタンもセンサーも付いていない。**自分で部品をつなぐ**」
- 「1枚290円、日本の個人が作ったボード」というストーリー（つかみとして強い）
- 基板の各部：USB-C／RESETボタン／電源LED・USB LED・ユーザーLED／ピンの並び／3.3V-5V切替
- 💪 自分のボードのどこに何があるか、指さして確かめよう

### i2 Arduino IDE をじゅんびしよう

1. Arduino IDE 2.x をインストール（当日は**インストール済みPC**を推奨。時間短縮）
2. `ファイル → 基本設定 → 追加のボードマネージャのURL` に
   `https://github.com/YuukiUmeta-UIAP/board_manager_files/raw/main/package_uiap.jp_index.json` を貼る
3. `ツール → ボード → ボードマネージャ` で「**uiap**」を検索してインストール
4. `ツール → ボード → UIAPduino → Pro Micro CH32V003` を選ぶ
5. ⚠ Windows は Visual C++ 再頒布可能パッケージ（64bit）が必要な場合あり
- ⚠ **シリアルポートは選ばない**（このボードは COM ポートとして見えない）

### i3 はじめてのLチカ

**書き込み手順（このワークショップ最大のつまずきポイント。図解必須）**

1. USBケーブルを抜いた状態で **RESETボタンを押しながら** USBを挿す
2. すぐに RESET を離す（青いPWR LEDが点いていることを確認）
3. Arduino IDE の「→（書き込み）」を押す
4. `Image written.` が出たら **RESETボタンを1回押す** → プログラムが動き出す

プログラムの形：`setup()` は1回だけ／`loop()` はずっとくりかえし

```cpp
// はじめてのLチカ
const int LED = 2;          // 基板のオレンジLED

void setup() {
  pinMode(LED, OUTPUT);     // このピンは「出力」に使うよ
}

void loop() {
  digitalWrite(LED, HIGH);  // 光らせる
  delay(500);               // 0.5秒まつ
  digitalWrite(LED, LOW);   // 消す
  delay(500);
}
```

- 💪 `delay` の数字を変えて、速く・ゆっくり点滅させてみよう
- 💪 SOS（トン・ツー）を光で送ってみよう

### b1 ブレッドボードにLEDをつなごう

- ブレッドボードの穴のつながり方（縦5穴が1組／電源レールは横一列）を図で
- LED の足の長さ＝＋、抵抗は **1kΩ**、GND へ戻す
  - ⚠ Arduino 定番の 220Ω は AVR（20mA駆動）向けの値。CH32V003 には過大

## 作品編（`p`）の設計メモ（2026-09-20 検討）

e6_project を1章で扱うには大きすぎるため、**新セクション「作品編」**として
独立させることにした。構想から発表までを 2〜3回（各3時間）でガイドする。

### 章立て（案）

| 章 | タイトル | 核 |
|---|---|---|
| p1 | なにを作るか決めよう | アイデアの出し方と、**切り方** |
| p2 | 設計図をかこう | 見た目・配線・動きの順番の3枚 |
| p3 | 材料と道具をあつめよう | 電子部品と**工作材料**の両方 |
| p4 | 電源をどうするか | 独立した章。失敗が本番で出るため |
| p5 | 骨格をつくろう | **動くものは土台が9割** |
| p6 | くみたてよう | 少しずつ足して確かめる |
| p7 | 発表しよう | 苦労も含めて語る |
| p8 | つぎの一歩 | UIAPduino の限界と、次の道具 |

**執筆ずみ**：p1・p2・p3・p4・p5・p6・p7・p8（全8章）。e6_project は解体して吸収した。

**決定事項**
- 既存の `e6_project` は**解体して p1〜p7 に吸収**する
- 工作材料は**決め打ちしない**。選択肢と長所短所を示し、
  **100円ショップでアイデアを探す**よう促す（材料探しも設計行為）
- 安全上の注意は**しっかり書く**（グルーガンのやけど、電源の誤接続）

### p4 電源 ─ 公式の安全表（2026-09-20 実物で確認）

公式ページ「ボードへの電力供給条件の組み合わせ」より。

| 組み合わせ | 判定 |
|---|---|
| 外部給電 ＋ **USB給電あり** | **危険** |
| 外部給電 ＋ USB給電なし | 安全 |
| **外部給電3.3V ＋ 外部給電5V**（同時） | **危険** |
| 電圧選択（3.3V / 5V）× 外部給電 | USB給電なしなら安全 |
| 共通GND接続 | 上の条件を満たしていれば安全 |

> \*1 共通GND接続です。
> \*2 安全ですが、外部給電が3.3Vの場合は5Vを使用できません。
> \*3 安全ですが、5Vは使用できません。

→ **教材に書く鉄則：「電池で動かすときは、USBをかならず抜く」**
   電池をつないだままUSBを挿す事故は、子どもなら確実にやる。

**電源構成の方針**

| 用途 | 構成 | 備考 |
|---|---|---|
| 動かないもの | PCのUSB ＋ モータ用電池 | e4・e5 の延長。ボードへの外部給電ではないので安全 |
| 動き回るもの | **モバイルバッテリー** ＋ モータ用電池 | USB給電なので同時接続の危険がない |
| （代替） | 単3×3 を 5Vピンへ | USBを抜く必要あり。4.8V→3V と下がるので後半不安定 |

⚠ **5Vピンに入れていいのは 5.5V まで**（絶対最大定格 VDD−VSS = 5.5V）。
   本数ではなく**電圧の上限**で教える。電池の種類で本数が変わるため。

| 電池 | 計算 | 可否 |
|---|---|---|
| アルカリ 1.5V × 3本 | 4.5V | ○ |
| 充電池（エネループ等）1.2V × 4本 | 4.8V | ○ |
| **アルカリ 1.5V × 4本** | **6V** | **✗ こわれる** |

**電池ボックスは4本用が圧倒的に多い**（3本用は入手しにくい）ので、
「3本」とだけ書くと4本用ボックスに素直に4本入れる事故が起きる。
→ **4本用ボックスを使うなら充電池**、という正解を示しておく。

⚠ **モバイルバッテリーの小電流オートオフ**：ボードの消費は約11mA で、
   一般的なしきい値（50〜100mA）を下回るため、給電が止まる機種がある。
   **挙動は機種によって大きく違う**（実地で確認ずみ）。

   → **検証しない方針**。生徒が持ってくるのは別の機種なので、
     こちらで1台試しても一般化できない。教材には**注意書きとして書く**：
     「止まってしまうモバイルバッテリーがある。動かないときは別のものを試す。
       モーターを積んだ作品なら消費が大きいので止まりにくい」

### p5 骨格 ─ 材料と留め方の知見

**紙・ボード**

| 材料 | 長所 | 短所 |
|---|---|---|
| 段ボール | 手に入りやすい | **切断が難しい／筋方向にヘタる** |
| 厚紙 | はさみで切れる | 水と力に弱い |
| スチレンボード | 軽く、カッターでまっすぐ切れる | 力がかかると折れる |
| 牛乳パック | 固い | **つるつるで接着しにくい** |

**プラスチック**

| 材料 | 長所 | 短所 |
|---|---|---|
| ペットボトル | 軽くてじょうぶ | 形の加工が難しい |
| プラ箱（100円ショップ） | 安価で そのまま使える | 切る・穴あけが難しい |
| プラバン | **テープがよく止まる** | 加工と接着に慣れが要る |
| 卵パック | でこぼこが そのまま使える | 薄くてつぶれやすい |

**棒・板**

| 材料 | 長所 | 短所 |
|---|---|---|
| **割りばし ＋ 結束バンドで十字** | **構造材として最強** | — |
| 竹串 | **回転軸に良い** | 構造材には弱い |
| ストロー | 曲がる・筒として使える | 力には弱い |
| かまぼこの板 | **重いので土台に向く**（p5 の「土台が うでに負ける」への答えの1つ） | 切るにはのこぎり |

3グループ×4種の計12種。**どれが最適とは書かない**方針（作品編は「経験にもとづく選択肢」）。
p3_parts は同じ3グループを箇条書きで挙げて p5 に誘導、p5_frame が `materials.png` で詳細を持つ。

| 留め方 | 長所 | 短所 |
|---|---|---|
| グルーガン | **固まるまでが速い** | やけど注意 |
| 超強力両面テープ | **剥がすと土台が壊れるほど強力** | やり直せない |
| 結束バンド | **棒状どうしに有効** | **ズレに弱い** |

**ブレッドボード → ユニバーサル基板（下書き→清書）** ─ 出典確認ずみ

- 秋月「片面ガラス・ユニバーサル基板（ブレッドボード配線パターンタイプ）」
  通販コード **104303** / ¥170 / 80×50×1.6mm / ランドピッチ2.54mm
  商品ページに「パターンは **EIC-801と同一**」「ブレッドボードで製作した作品を
  **部品配置そのままに移植することができる**」と明記されている。
  <https://akizukidenshi.com/catalog/g/g104303/>
- 同系統は他サイズもあり（P-12356 56.5×32mm、112222 Cタイプ48×72mm など）。
  教材では**品番を書かず**「ブレッドボード配線パターン」という**名前で探す**と書いた。
  在庫・品番は変わるが、この呼び名は変わらないため。
- **電源レールは上下それぞれに ＋ と − の2本ずつあり**、この教材で使っている
  ブレッドボード（bb.py の `-t/+t/-b/+b`）と互換。商品ページには記載がないが、
  実物写真で確認ずみ。並び順も**上下とも 青（−）が外、赤（＋）が内**で bb.py と一致。
- 実物は**白い基板に金色のランド**（緑ではない）。中央にみぞ、左右に取りつけ穴、
  行の記号と列の番号が印刷されている。`draft_to_board.png` はこの見た目で描いてある。
- ブレッドボード配線パターンは、**基板の中の特殊な1種**という位置づけ。
  使う理由はあくまで**まちがえにくさ**で、5穴の組が固定されているぶん
  **スペース効率は悪い**。ふつうのユニバーサル基板のほうがずっと小さくまとまる。
- ⚠ **スライドの順序は 一般 → 特殊**。当初は「下書きと清書」の図に
  いきなり配線パターン基板を描いていたが、それでは特殊例を先に見せることになる。
  いまは ①下書きと清書（ブレッドボード → **ふつうの基板**）→
  ②同じ並びの基板もある（**ふつうの基板** と 配線パターンをならべる）の順。
  図②は「変える」のではなく「ならべてくらべる」ので、**矢印を出さない**
  （点線の仕切りにしてある）。
- 図②の配線パターン側は、**たて5つをつなぐ銅のパターンを太い帯で描く**。
  細い線では「つながっている」が読み取れず、ふつうの基板との差が出ない。
  帯の上でもランドが見えるよう、あなを大きめ・暗め（半径3.4／#684C1E）にしてある。
- ⚠ 図の縦横比：1340×660（2.03）。`materials.png` の2.0と同じ帯。
  **1340×730（1.84）にしたらキャプションがスライドからはみ出した**ので、
  この章の図は 2.0 前後を目安にする。

**p5 の骨子は2つ**（当初は3つにしていたが、実地の指摘を受けて整理した）

**① 土台が うでに負ける**
うでを動かす力と同じ力が、反対向きに土台へかかる。直し方は2通り。
- うでと**同じくらい**、土台をのばす
- **逆がわ**にのばして、**おもり**か**テープ**でおさえる
  （電池を土台に置くとおもりのかわりになる／養生テープで机に貼る）

**② とめ方が弱い** … 具体的な3つのコツ
- **広い面でとめる**（小さく1か所より、大きく1面）
- **結束バンドは直角に2本**（1本だとくるっとずれる）
- **でこぼこはグルーガン**（すき間をうめて固める）

※ 当初は「軸だけで支える → 反対側にも支点」という項目を立てたが、
  **意図が伝わらない**と指摘された。上の①に統合し、
  「土台をどう作るか」という具体的な話に置きかえた。

**サーボの固定のしかた**（実地の知見）

- ⚠ **耳（取付フランジ）のねじ止めは、子どもの紙工作では難しい**。
  下穴を開ける・ねじを締めるのが工具と力の面でハードルが高い
- → **筐体の広い面に両面テープで貼る**のが現実的。
  サーボは側面が平らなので、面で接着できる

**p5 の検証について**：材料と構造の組み合わせが無数にあるため、
こちらで1例を作っても一般化できない。**選択肢と失敗パターンを示す**方針とし、
実地の検証はしない。

**ブレッドボードの卒業**：持ち歩く作品では振動で抜ける。
両面テープで固定するか、ユニバーサル基板にはんだ付けするか。
後者を選ぶと、いま孤立している付録④「はんだ付け」に意味が生まれる。

### p8 つぎの一歩

UIAPduino の限界を正直に書く（16KB / シリアルモニタなし / PWM 5本 /
アナログ 5本 / 無線なし）。そのうえで「作りたいものに合わせて道具を選ぶ」
という**選定の考え方**を教える。

Arduino UNO R4（情報量・シリアルモニタ）／ESP32（Wi-Fi）／
micro:bit（センサー内蔵）／Raspberry Pi（カメラ・画面）。

**「ここで書いたプログラムは、次のマイコンでもほぼそのまま動く」**と
伝えられるのが、Arduino を選んだことの回収になる。

---

### e1 を「つまみでさかいめを決める」形に作りかえた（2026-09-23）

実機のつまずきを受けての作りかえ。**ねらいは、章の途中で配線を組みかえないこと。**

**① 固定の 10kΩ をやめ、そこに 10kΩ の可変抵抗を入れる**
   `＋ ── CdS ── A2 ── つまみ ── −`。つまみは**まん中と片はしの2本**だけ使う。
   - いっぱいに回しきる ＝ 10kΩ ＝ もとの回路と同じ → 値を見るページで使う
   - 少しもどす ＝ 室内でちょうど さかいめになる → 自動ライトのページで使う
   → **配線は1回で終わり**。ページごとに組みかえる必要がない。

**② 明るさ → 点滅の速さ に変えた**
   `analogWrite(値/4)` だと 205→37 で明るさ5.5倍あるが、
   **人の目は明るさを対数的にしか感じない**ので差が分かりにくい。
   `delay(値/4)` の点滅なら「速くなった」はすぐ気づける。

**③ 数で読むページを足した**（`e1_kazu`）
   `analogRead(CDS) / 100` の回数だけ基板のLEDを点滅させる。
   明るいと8回・おおうと1回。`verify/v12_analogshow` の教材版（14行に収めた）。

**④ しきい値はコードに固定（500）し、つまみのほうで合わせる**
   数字を探させると部屋の明るさで変わってしまう。
   「明るいまま、ぎりぎり消えるところまで回す」のほうが体でわかる。

図は `tools/fig_bb_cds.py`。長い線は i行、レールへの立ち上げは j行からにすると
立ち上げが横の線をまたがないので交差が出ない。

### e1 CdS の実測値と、しきい値を 300→500 に（2026-09-23）

**実測**（固定抵抗 10kΩ・`5V ── CdS ── A2 ── 10kΩ ── GND`）

| 状態 | 読み値 | 逆算した CdS の抵抗 |
|---|---|---|
| 明るい（室内） | **822** | 2.5 kΩ |
| 指を1本のせる | 526 | 9.5 kΩ |
| **手でおおう（すきまなく）** | **149** | 58.7 kΩ |

振れ幅 **673（全体の66%）**。→ **しきい値は 300 では低すぎた**ので、
2つの値のまん中に近い **500** に変更した。

**⚠ 固定抵抗は 10kΩ のままでよい。** この CdS の明暗の抵抗から計算した
最適値は **12.0kΩ**（相乗平均）で、10kΩ はほぼそのもの。

| 固定抵抗 | 振れ幅 |
|---|---|
| 1kΩ | 279 |
| 4.7kΩ | 596 |
| **10kΩ（採用）** | **673** |
| 22kΩ | 642 |

**つまずきの原因は遮光不足だった**。「指を1本のせる」では 526 までしか
下がらず（振れ 296）、さらに `analogWrite(値/4)` の 205→131 は
**明るさにして1.6倍**しかないため、目にはほとんど同じに見える。
人の目は明るさを対数的にしか感じないので、**値が3割動いても気づけない**。

→ 本文に「手をカップのように丸めてかぶせる」「指1本では変わらない」を明記。
   しきい値も決め打ちをやめ、「2つの値のまん中」で決めさせる書き方にした。
   付録②にも「反応が小さいとき」を追加（まず2つの値を測る）。

**⚠ 切り分けの教訓**：途中で 526 を「暗いときの値」として計算し、
「固定抵抗を 4.7kΩ に変えるべき」と誤った結論を出しかけた。
**測定条件が正しいかを先に確かめる**こと。部品を疑う前に測り方を疑う。

### e4 サーボの配線を レール経由 にした（2026-09-23）

電池からサーボへ直接つないでいたのを、**電池 → 下の ＋−レール → サーボ**
という形に変えた。道すじが1本の線としてたどれるようになる。

- サーボの3本は **となりあう列（24・25・26）にいったん落として**から配る。
  連結器から直接レールへ引くと、**信号線（黄）が電源の2本と交差する**
  （黄はいちばん下の線なのに、いちばん高い場所＝5番ピンへ行くため）。
  いったん同じ行に落とせば、そこから先は短い縦の線だけで済む。
- 電池の ＋は **27列**、−は **30列**。`+b` レールは `-b` より下にあるので、
  **＋の列を −の列より左**にしないと、2本が交差する。
- ボードの GND も同じ `-b` レールへ（共通GND）。

作図は `tools/fig_bb_servo.py`。

### HC-SR04 の仕様（2026-09-23・データシートで確認）

Elecfreaks の Ultrasonic Ranging Module HC-SR04 データシートより。
<https://cdn.sparkfun.com/datasheets/Sensors/Proximity/HCSR04.pdf>

| 項目 | 値 |
|---|---|
| 測定範囲 | **2cm 〜 4m** |
| 測定角 | **15度** |
| 精度 | 3mm |
| 電源 | DC 5V ／ 15mA |
| 超音波の周波数 | 40kHz |
| TRIG に入れる合図 | **10µs 以上の High** |
| 寸法 | **45×20×15mm** |

- 教材の `digitalWrite(TRIG,HIGH); delayMicroseconds(10);` は、この
  「10µs 以上」の要件をちょうど満たしている。
- ⚠ **2cm より近いと測れない**。P3 に注意書きとして入れた。
- ピンの並びはデータシート表記が `Vcc Trig Echo GND`（**裏面から見た順**）。
  教材は**上から見た図**なので、左から **Gnd・Echo・Trig・Vcc** と書く。
  P3 の「足は4本。VCC・TRIG・ECHO・GND」も上から見た順に直した。

### e3 の 💪 に3色LEDの課題を足し、ピンの衝突を直した（2026-09-23）

💪 に「30cmで緑・20cmで緑＋青・10cmで緑＋青＋赤」を追加。
近づくほど増えていくので、しきい値を3つ並べる練習になる。

**⚠ ピンの衝突を2件見つけて直した**

- 💪「ブザーは **5番**へ」… **5番は本編のLED**。→ **4番**に変更
- c4 の3色LEDは 5・7・8番だが、**e3 は 7番が ECHO**。
  → e3 では **赤5・緑8・青10** に割りあてる（c4 とは別の組み合わせ）

e3 で使うピン（重複なし）：9=TRIG／7=ECHO／5=赤／8=緑／10=青／4=ブザー。
避けたピン：2（基板のLED）・11（SWIO）・3（I2Cのプルアップあり）。

→ **章ごとに使えるピンは違う**（前の章の割りあてをそのまま持ちこめない）。
   💪 で部品を足す課題を書くときは、その章の本編が使っているピンと
   必ず突き合わせること。

### HC-SR04 は j行にさす（2026-09-23）

本体が **45×20mm**。20mm は **約8行ぶん**あるので、どの行にさしても
まわりの行をおおってしまう。f行にさすと g〜j行が使えなくなる（実機で確認）。

→ **いちばん外の j行**にさす。こうすると本体は**盤面の外へはみ出す**ので、
   f〜i行があいたまま残り、4本の線がどの穴に入るか図で見える。

- **ピンは左から Gnd・Echo・Trig・Vcc**（2026-09-23 修正）。
  端子名は部品の**裏**に書いてあるので、**上から見ると左右が逆**になる。
  最初 Vcc・Trig・Echo・Gnd で描いていたのは、裏側から見た並びだった。
- 列は **18〜21**。本体（45mm＝17.7列ぶん）が col 30 にかからないようにして、
  上下の ＋レールをつなぐジャンパーを col 30 に通す余地を残した。
- 合図は i行（Echo）と h行（Trig）に平行に流す。
  電源はセンサー本体が上のレールにかぶるので、**f行から下のレールへ下ろす**
- LED は基本編と同じ **16・17列**の外づけ（8番ピンから）。下半分はぜんぶあく
- ⚠ 図は**上から見た形**にしてある。真横から描くと盤面に重ねられない
- 作図は `tools/fig_bb_sr04.py`

### e5 を PHASE/ENABLE モードに変え、2モーターを本編にした（2026-09-23）

**きっかけ**：2つめのモーターのピンを検討したところ、**PWM が足りない**。
実機で PWM が使えるのは 2・5・6・12 の4本だけ（2は基板LED、5は下の行）。
IN/IN は1モーターに PWM 2本いるので、2モーターで4本＝破綻する。

**解**：`MODE` を HIGH にして **PHASE/ENABLE モード**にする。

| モード | 1モーター | 2モーター |
|---|---|---|
| IN/IN | PWM 2本 | PWM 4本 ✗ |
| **PHASE/ENABLE** | PWM 1本＋digital 1本 | **PWM 2本＋digital 2本** ✓ |

資料の真理値表（MODE=1）：`ENABLE`=0 → ブレーキ（PHASE 無関係）、
`ENABLE`=1 かつ `PHASE`=1 → 正転、`PHASE`=0 → 逆転。
**PHASE が向き、ENABLE が速さ**と役割が分かれるので、説明も素直になった。

**ピン**：APHASE=7 / AENBL=12(A3) / BPHASE=9 / BENBL=6(A2) / MODE=＋レール。
💪 のつまみは A1(0番) へ（0番は PWM 不可だが `analogRead` は可）。

**はじめから2モーター前提で組む**ようにした。A だけで動かしたあと、
**配線を組みかえずに B を足せる**。これが PHASE/ENABLE を選ぶ最大の理由。

**ページ構成**：本編に「モーターを2つにしよう」「Bをつなぐ」「左右で動きを変える」
「2つ動かすプログラム」の4枚を追加（💪 の前）。ロボットカーは作品編で必須の
工作なので、やってみようではなく本編に置いた。

⚠ **図の交差について方針が変わった**（ユーザー判断）。ジャンパーは
「どこからどこへ」が分かればよく、**途中の経路は気にしない**。
空中配線が交差しても子どもは理解できる、とのこと。
これにより、これまで交差を避けるために苦労していた配線図の制約が外れた。

### ⚠ e5 の逆転が弱い（2026-09-23〜24・**原因はモーター端子の断線**）

**症状**（PHASE/ENABLE に変えたあと。前節の「逆転しない」とは別の症状）

1ループで **3つ以上の動き**が見える：
- リセット後 **最初は弱い回転**
- **すぐに強い回転に変わる（同じ向きのまま）**
- 少しして **強い逆向き**
- そのあと **弱い正回転** が2秒ほど
- 回らずに **「ピー」と鳴る**こともある

**いちばん重要な手がかり**：`loop()` は駆動を**2回**しか指示していないのに
動きが3つ以上ある。しかも **1回の指示の途中で強さが変わる**。
`analogWrite(AENBL, 200)` を出したきり何も命じていない区間で強さが変わる以上、
**変わっているのは APHASE ではなく AENBL 側（または電源）**。

**つぶした仮説（すべて否定ずみ。同じ道を二度たどらないこと）**

- ❌ **7番の HIGH が VIH に届かない** … 実測で 正転中0V／逆転中5V。
  **このボードは既定5V動作**（3.3Vではない）なので、どちらも正常な LOW／HIGH。
  そもそも VIH = 0.5×VCC = 2.5V なので 3.3V でも足りていた。
- ❌ **モジュールのプルアップ** … AE-DRV8835-S の実装部品は
  **DRV8835 と パスコン2個だけ**。抵抗のフットプリントすら無い（PDF の外形図で確認）。
  ブロック図の抵抗記号は **IC内蔵の100kΩプルダウン**であって外付けではない。
- ❌ **配線の取りちがえ（AIN1/AIN2 の役割）** … TI データシートの
  Pin Functions・Pin Configuration・秋月の図、**3つとも一致**：
  **AIN1(10番ピン)=APHASE（向き）／ AIN2(9番ピン)=AENBL（速さ）**。教材の配線は正しい。
- ❌ **7番に残った PWM** … `v04_pwm_scan` は7番にも `analogWrite` するので
  疑ったが、**9番に移しても症状が同じ**（v18 TEST3）。`analogWrite(7,0)` を
  明示しても同じ（TEST2）。
- ❌ **急な反転による過電流保護** … OCP は 1.6〜3.5A で落ちて**約1ms で自動復帰**。
  これでは **弱いまま2秒つづく**ことを説明できない。
  （向きを変える前に止めて0.5秒待つ v18 でも変化なし）
- ❌ **ボードの5Vピンの 0.5A 制限** … VM は**モーター専用の別USB電源**から
  ＋−レールに供給されている。ボードの5Vピンは経由していない。

- ❌ **GND が別々** … モーター電源の − と マイコンの GND は
  **つながっている**と確認ずみ（2026-09-23）。基準ずれではない。

**残っている可能性（ここから再開）**

向き（APHASE）側は**ほぼ白**。GND も共通。残る変数は
**AENBL の PWM そのもの**と**電源の供給能力**の2つ。

1. ✅ `verify/v19_enbl/` … **モーターを外し、12番にLEDだけ**をつないで PWM を見た。
   **結果（2026-09-24）**：2秒点灯・0.5秒消灯を**指示どおりくりかえした**。
   → **マイコンの PWM 出力は正常**。AENBL 側も白。
   次は `verify/v20_nopwm/` … AENBL を PWM ではなく**ただの HIGH（全力）**にして回す。
   両方向とも力強ければ、**PWM のオン・オフと別USB電源の相性**（電源の保護動作）。
   まだ片方が弱ければ、PWM は無関係で**モーター自身か電源の容量**。
   ✅ **v20 の結果（2026-09-24）**：**両方向とも力強い**。
   → 原因は **PWM（既定 1000Hz）× モーター用USB電源** の組みあわせにしぼられた。
   「ピー」音は 1000Hz の PWM そのものと考えられる。
   次は `verify/v21_pwmfreq_motor/` … `analogWriteFrequency(20000)` で 20kHz にする。
   コイルが電流をならすので、電源から見るとほぼ一定の電流になるはず。
   あわせて**単3電池4本**でも 1000Hz のまま試し、電源の種類で変わるかを見る。
   ✅ **v21 TEST 2（20kHz）の結果（2026-09-24）**：**両方向とも力強い**。
   ✅ **v21 TEST 1（1000Hz・既定）の結果**：**これも両方向とも力強い**。
   → **「PWM × USB電源」説は成り立たない**。周波数を変える必要はない。

**⚠ 真相（確定）：モーターの端子が断線しかけていた**

調査の途中で、**それまで使っていたモーターの端子が断線**し、同じ仕様の
別のモーターに入れかえていた（ユーザー申告・2026-09-24）。新しいモーターでは
1000Hz のままでも正常に回る。**端子が切れかけて接触したり離れたりしていた**と
考えると、症状がすべて説明できる：

- **1回の指示の途中で強さが変わる**（マイコンは何も命じていない）
- 回らずに **「ピー」**（PWM の音だけ出て、電流が流れない）
- ピンや書き方を変えても**直らない**（原因がマイコン側に無いから）

**→ 教材のプログラムは直さない。** `analogWriteFrequency` を足す案は取り下げ。
✅ **入れかえの時期**：**v20 の前**（9月24日の最初のモーター試験から新しいモーター）。
つまり **不調はすべて古いモーター、正常はすべて新しいモーター**での結果。
→ **原因は古いモーターの端子で確定**。v20・v21 は PWM や電源について
何も切り分けていない（どれも新しいモーターなら正常に回っただけ）。
残りの確認：e5 P9 のプログラムをそのまま新しいモーターで回す
（止めずに反転するので、念のため）。
✅ **e5 P9 のプログラムを新しいモーターでそのまま回して問題なし**（2026-09-24）。
→ **e5 のモーターA（PHASE/ENABLE・APHASE=7・AENBL=12）は実機確認完了**。
✅ **モーターB（BPHASE=9・BENBL=6）を足した「2つ動かすプログラム」も問題なし**（2026-09-24）。
→ **e5 は2モーターまで実機確認完了**。

**→ 教訓：命令が一定なのに動きが変わるなら、まず接触不良を疑う。**
マイコンは同じ命令を出しつづけているので、変わりうるのは物理的なつながりだけ。
今回はピン・GND・PWM・電源と電気の仮説を7つ順に消したが、
最初に**モーターを別のものに替えてみる**のがいちばん安い切り分けだった。
モーターの端子（はんだづけのタブ）は曲げに弱く、**くり返しの抜き差しで切れやすい**。
2. **電源の供給能力**。「別USB電源で安定」でも、**アダプタの定格A数**が
   足りなければ電圧は落ちる。5V/0.5A のアダプタだと DCモーターには足りない。
   モーターが回っている最中に **VM と GND の間の電圧**を測るのが決定的。
   5Vを保っていれば電源は白。3V台に落ちていれば電源が原因。
3. **モーターそのもの**。ブラシDCモーターは向きで特性が変わることがある。
   モーターの2本の線を**入れかえて**同じ症状が出るか。
   症状が**向きについてくる**ならモーター、**向きと無関係**なら回路側。
   （これは部品を疑う前の切り分けとして安い）
4. ドライバICの発熱。TSD（150〜180℃）で落ちて自動復帰していないか。
   指で触れて熱いかどうかだけでも分かる。

---

### 付録⑨「実機とコアの落とし穴」を作った（2026-09-23）

公式ピン配置図どおりに使えない点と、コアの不具合を1か所にまとめた。
**この章だけ大人むけ**（教材を作る人・改造する人が読む）。

収めた内容：PWM は4本だけ（0・1・10 は無反応）／`digitalWrite` で PWM が
止まらない／`pinMode` に出力の種類はない／アナログ入力は5本（A6 不可）／
`pulseIn` が使えない／シリアルモニタが使えない／`analogWrite` の既定は12bit・
`LED_BUILTIN` 未定義・`Servo` 同梱なし・13/14/17 欠番・11 は SWIO。

それぞれに**なぜそうなるか**（`PinMap_TIM` の相補出力、`PinMap_ADC` の記述ミス、
`turnOffPWM` の不在、`micros()` の分解能）と**迂回策**を書いてある。

最後に「パッケージを更新したら確かめ直す」ページを置いた。どの `verify/`
スケッチを走らせればよいかを並べてある。将来直ったときに気づけるように。

x3（ピン配置早見表）・x7（メンター早見表）・CLAUDE.md から参照している。

### 温度センサーが MCP9700 ではなく MCP9701 だった（2026-09-23）

**症状**：`e2_temp` で、しきい値を 28 にしても 38 にしても LED が最初から点いたまま。

**原因**：手元の実物は **MCP9701**。教材の式 `(volt - 0.5) * 100` は **MCP9700 用**だった。
9701 は 0℃の出力も1℃あたりの傾きも違うので、室温25℃で **38.9℃** が出ていた。

| 部品／状態 | 25℃での `analogRead` | 教材の式が出す温度 |
|---|---|---|
| MCP9700（想定していた部品） | 154 | 25.2℃ → 消灯 |
| **MCP9701（実物）** | **182** | **38.9℃ → 点きっぱなし** |
| 出力が 5V に張りつき | 1023 | 449℃ |
| ボードが 3.3V 動作 | 233 | 63.8℃ |

**しきい値をどう変えても点灯したまま**という症状から、値そのものが大きすぎると分かり、
候補ごとに「25℃のときいくつになるはずか」を並べて 182 に当たりをつけた。

**⚠ 教訓**：CURRICULUM に「MCP9700-E/TO に**確定**」と書いてあったが、
それは**商品ページを見て決めただけ**で、**現物の印字を確かめていなかった**。
データシートの確認と、手元の部品がそれと同じかの確認は**別の作業**。
型番の末尾1文字で特性が変わる部品（9700/9701、SB412A/SB612A など）ではとくに注意する。

→ 実物の印字を確認するまで「確定」と書かない。確認したら**確認日と確認方法**を書く。

**直したもの**：`e2_temp` の本文とコード2か所・`x1_parts` の部品リスト・この記録。
式はミリボルトで計算する形にした（小数が減って説明しやすい）：
`(v * 5000.0 / 1024 - 400) / 19.5`

`analogRead` の生の値を LED の点滅で読む道具を `verify/v12_analogshow/` に置いた。
「センサーの値がおかしい」ときの最初の一手になる。

### 「ECHO は必ず7番」という記述の誤りを訂正（2026-09-20）

e3 に「⚠ **ECHO は必ず7番**に。ほかのピンだとボードを傷める可能性があるよ」と
書いていたが、**根拠のない記述**だった（検証記録にも理由がない）。

データシート（V1.3 §3.3.9）の入力電圧仕様:

| 種別 | VIH 最大 |
|---|---|
| Standard I/O | **VDD + 0.3V** |
| FT（5Vトレラント）I/O | 5.5V |

**このボードは既定 5V 動作**なので、標準ピンでも 5.3V まで許容する。
HC-SR04 の ECHO（5V）は、どのピンでも規格内に収まる。
→ 「7番でなければ壊れる」は誤り。7番は単なる選択。

**3.3V に切りかえたときだけ**、FT ピンかどうかが問題になる。
教材では 3.3V 動作を扱っていないので、そこには触れていない。

記述を「HC-SR04 は 5V の信号を出す。このボードは 5V で動いているから、
そのまま受けとれる」に変更した。

### ⚠ `analogWrite` のあと `digitalWrite(LOW)` で出力が止まらない（2026-09-23）

**v16 で確定**。テストLEDを 12番・0番 に挿して

| 段 | やったこと | 見えたこと |
|---|---|---|
| ① | `analogWrite(12,200)` | 12番 点く |
| ② | `digitalWrite(12,LOW)` ＋ `analogWrite(0,200)` | 0番も点く。**12番が消えない** |
| ③ | `analogWrite(12,200)` ＋ `digitalWrite(0,LOW)` | **0番も消えない**。両方つきっぱなし |

→ **`analogWrite` を出したピンは、`digitalWrite(LOW)` では止まらない。**

**これで e5 の症状がぜんぶ説明できる**

```
正転  analogWrite(AIN1,200)                 → 回る
逆転  digitalWrite(AIN1,LOW)  ← 効かない → AIN1 は 200 のまま
      analogWrite(AIN2,200)                 → AIN2 も 200
      両方 High 相当 ＝ 真理値表の「1 1」＝ **ブレーキ**
停止  digitalWrite 両方 LOW   ← 効かない → ブレーキのまま
```

最初の正転のあと**ずっとブレーキ**がかかるので、二度と回らない。
`loop()` は回りつづけている（v14）し、正転だけなら毎回回る（v15）という
観察とも矛盾しない。

⚠ **切り分けの反省**：最初に「0番の PWM が効かないのでは」と見当をつけたが、
5秒周期で再び回らない時点でその説明では足りなかった。**症状の全部を
説明できる仮説か**を先に確かめるべきだった。v15 の結果を「正転は正常」と
読んだのも早く、実際は v16 を走らせていたという行きちがいもあった。
**いま何のスケッチが動いているかを、毎回確かめてから結果を解釈する。**

**v17 の結果（2026-09-23）**

| 止め方 | 結果 |
|---|---|
| `digitalWrite(pin, LOW)` | ❌ **止まらない** |
| `analogWrite(pin, 0)` | ✅ 止まる |
| `pinMode` を入れ直して `digitalWrite(LOW)` | ✅ 止まる |

あわせて **0番(PA1) は `analogWrite` 無反応**と判明（テストLEDが一度も点かない）。
1番・10番と同じ。**PWM が実際に使えるのは 2・5・6・12 の4本**。

**直したこと**
- `AIN2` を **0番(A1) → 6番(A2)** に（0番は PWM 不可のため）
- 止めるのを `digitalWrite(LOW)` → **`analogWrite(0)`** に
- 真理値表の説明を「高い／低い」→「大きい数／0」に（コードと一致させた）
- 💪「つまみでスピード」の つまみを **A0(1番)** へ逃がした（A2 はドライバが使う）
- 配線図・拡大図の AIN2 を 6番(A2) に

**⚠ これは Arduino の仕様ではなく、このコアの不足**（ソースで確認）

公式 AVR コアの `digitalWrite` は、PWM 対応ピンなら
`if (timer != NOT_ON_TIMER) turnOffPWM(timer);` で**先に PWM を止めてから**
ポートに書く。だから標準の Arduino では `digitalWrite(LOW)` で止まる。

UIAP コア（ch32v 1.0.42）の `digitalWrite` は `digitalWriteFast` を
呼ぶだけで、`turnOffPWM` に相当する処理がない。タイマーが PWM を
出しつづけるので、GPIO に書いても出力が変わらない。

なお `pinMode` に「デジタル出力／アナログ出力」の区別はない
（`INPUT` / `OUTPUT` / `INPUT_PULLUP` の3つだけ）。`analogWrite` は
内部でタイマーを設定するので、**後始末は `digitalWrite` 側の仕事**という
のが Arduino の設計。そこが抜けている。

`pulseIn` が使えない・`analogRead(A6)` が 0 を返すのと同じ、
**コアの作り込み不足**。将来のパッケージ更新で直る可能性がある。

**→ 教訓：このボードでは `analogWrite` と `digitalWrite` を同じピンに混ぜない。**

（旧メモ）つぎは `verify/v17_pwmoff/` で止め方を3とおり試す
   （`digitalWrite(LOW)` ／ `analogWrite(0)` ／ `pinMode` を入れ直す）。
   効く書き方が分かれば、e5 のコードをその形に直す。

### ⚠ e5 のモーターが逆転しない（2026-09-23・調査中）

**症状**：リセット後2秒だけ回り、それ以降回らない。

`loop()` は 正転（`analogWrite(AIN1,200)`）→ 逆転（`analogWrite(AIN2,200)`）
→ 停止 のくりかえし。**AIN2＝0番の `analogWrite` が無反応**だと、
正転の2秒だけ回って、あとは何もしないように見える。症状と一致する。

**根拠**：v04 で **1番(PA2)・10番(PD0) は `analogWrite` が無反応**と実測ずみ。
どちらも TIM1 の**相補出力**（CH2N/CH1N）で、MOE ビットを立てないと端子に出ない。
**0番(PA1) は未検証**のまま残っていた（`verify/README.md` に「未検証」と明記あり）。

→ `verify/v13_pwm0/` で 12・0・6番の PWM を見くらべる。

**追報（2026-09-23）**：「一度止まると **5秒以上たっても二度と動かない**」と判明。
`loop()` は 2+2+1＝**5秒周期**なので、生きていれば必ずまた回る。
つまり **PWM の問題ではなく、`loop()` 自体が回っていない**。

切り分けは `verify/v14_alive/`（モーターを外し、基板のLEDで loop を見る）。

| 見え方 | 意味 |
|---|---|
| くりかえす | loop は回っている → **モーターがわ**（電源・ドライバの保護・配線） |
| 1回で止まる | **マイコンが止まっている**（電源が落ちた／リセット） |
| 最初から光らない | 書きこみ後の RESET を押していない |

**v14 の結果（2026-09-23）**：✅ **くりかえした**。つまり `loop()` は回っており、
**マイコンは正常**。原因は**ドライバ〜モーターの側**にしぼられた。

資料の真理値表（IN/IN・MODE=0）は教材の記述と一致している。
| AIN1 | AIN2 | AOUT1 | AOUT2 | |
|---|---|---|---|---|
| 0 | 0 | HiZ | HiZ | 切りはなす（惰性） |
| 0 | 1 | L | H | 逆転 |
| 1 | 0 | H | L | 正転 |
| 1 | 1 | L | L | ブレーキ |

⚠ MODE が浮いて PHASE/ENABLE になっていると、AIN1=向き・AIN2=動かすか、に
変わる。その場合は**正転が止まり逆転が動く**ので、いまの症状（正転だけ回る）
とは逆。→ **MODE は効いている**とみてよい。

つぎは `verify/v15_motorstep/`（正転だけを3秒おきにくりかえし、
力の強さも 200 → 120 → 全開 と変える）で

| 見え方 | 意味 |
|---|---|
| 毎回回る | 正転は正常 → **逆転がわ（0番の analogWrite）**が怪しい → v13 へ |
| 1回きり | **ドライバのラッチか電池**。電源を入れ直して1回回るならラッチ |
| 弱めなら回りつづける | **電流が足りていない**（電池の消耗・接触不良） |

⚠ 電圧降下によるリセットなら、**また最初から2秒回る**はず。
「止まったまま」はそれとは症状が違う。DRV8835 の保護によるラッチ
（過熱・過電流で出力を止め、電源を切るまで戻らない）も候補。

**0番が使えなかった場合の案**

| 案 | 内容 | 問題 |
|---|---|---|
| 1 | AIN2 を 6番（A2）に | 💪「つまみでスピード」が A2 を使うのでぶつかる |
| 2 | AIN2 は `digitalWrite` だけに | 「逆転でもスピードを変えられる」が崩れる |
| 3 | **AIN1=12・AIN2=6 にして、💪 のつまみを A0(1番) へ逃がす** | A0 は `analogRead` 可（v05 実測ずみ）。つまみは入力なので PWM 不可でも困らない |

→ **案3 が有力**。ただし実測結果を見てから決める。

### e5 の AIN1/AIN2 を 5・6番 → 12・0番 に変更（2026-09-20）

DRV8835 は e行・f行 に溝をまたいで装着するので、合図の線は**上がわ**に集まる。
AIN1 が 5番（下がわにしかない）だと、溝をこえる3本のジャンパーが必要だった。

- **AIN1 = 12（A3）／ AIN2 = 0（A1）** に変更。どちらも上がわの PWM 可ピン
- PWM が使えるのは 0・2・5・6・12 の5本。うち上がわにあるのは
  **A3(12)・A2(6)・A1(0)** の3つだけ（A0(1) は相補出力チャンネルで PWM 不可）
- **A2(6) はあえて使わない**。e5 の 💪「つまみでスピードを決める」で
  b4 の可変抵抗が A2 を使うため、ぶつからないようにした
- 正転・逆転どちらでもスピードを変えられる、という教材の主張は維持

⚠ 実機検証は AIN1=5 で行ったもの。12番での再確認はまだ。
→ （2026-09-24）この IN/IN 構成は PHASE/ENABLE に置きかえた。AENBL=12 で実機確認ずみ。

### e3 の TRIG を 5番 → 9番 に変更（2026-09-20）

配線図を「センサーをブレッドボードにさす」形にしたとき、5番ピンが下がわにしか
ないため、TRIG だけ溝をこえる3本のジャンパーが必要になっていた。
**9番（上がわ）に変えると4本ともまっすぐ引ける**ので変更した。

- TRIG は 10µs のパルスを出す**ただのデジタル出力**なので、どのピンでもよい
- 9番は教材のどこでも使っていない（全章で確認済み）
- 教材の方針「PWMが使える貴重なピン（0・2・5・6・12）は、
  そこにしかできないことに使う」にも合う。TRIG に PWM は要らない
- 校正値 `cm = n - 2` は **ECHO の計測ループ**で決まるので、TRIG のピンを
  変えても影響しない

⚠ **実機検証は TRIG=5 で行ったもの**。9番での再確認はまだ。
  リスクは低いが、当日前に一度 v09k を 9番で走らせておきたい。

あわせて e3 の 💪 のブザーを **5番**に案内した（7番は ECHO が使うため）。
TRIG が 5番から外れたことで、5番があいた。

### 用意した LED と限流抵抗（2026-09-20 確認）

| 色 | 型番 | Vf | 光度 | 半減角 | 1kΩ での電流 | （330Ω なら） |
|---|---|---|---|---|---|
| 赤 | OSDR5113A | 2.0V | 1200mcd | 15° | **3.0mA** | 9.1mA |
| 黄緑 | OSNG5113A | 2.0V | 750mcd | 15° | **3.0mA** | 9.1mA |
| 青 | OSB5DA5A33A-1MA | 2.8V | 1120mcd | 30° | **2.2mA** | 6.7mA |

**教材は 1kΩ を採用**（2026-09-20 決定）。330Ω でも壊れないが、1kΩ のほうが良い：

- 3色とも **±8mA の「VOL/VOH を保証できる範囲」に確実に収まる**
- 3つとも高輝度（赤1200mcd・半減角15°）で、330Ω だと**直視がつらい**
- c4 の3個同時点灯が 25mA → **8mA** に下がる
- **1kΩ（茶・黒・赤）と 10kΩ（茶・黒・だいだい）は3本目だけが違う**ので、
  付録⑥のカラーコードの説明がきれいに決まる（教材上の副次的な利点）

CH32V003 データシート V1.3 §3.3.9 の原文：

> GPIO (General-Purpose Input/Output Port) can sink or output up to **±8mA** current,
> and sink or output **±20mA** current (**not strictly to VOL/VOH**).

→ **8mA は絶対最大定格ではなく「VOL/VOH を保証できる電流」**。±20mA までは流せて、
その場合は出力電圧の落ち込みが 0.4V → 1.3V に増えるだけ。
つまり 330Ω（9.1mA）でも壊れないが、1kΩ なら保証範囲に確実に収まる。

青は低電流・高効率タイプで Vf も 2.8V と低く、**「青は暗くなりがち」は当てはまらない**
（x1 の記述をこれに合わせて訂正した）。
- コードは i3 の `const int LED = 2;` を `5` に変えるだけ → **「配線を変えて、番号を変える」**という感覚をつかむ

### b2 スイッチでLEDをつけよう

```cpp
const int SW  = 3;   // タクトスイッチ（D3 = PC1）
const int LED = 5;   // LED（D5 = PC3）

void setup() {
  pinMode(SW, INPUT_PULLUP);  // 押していないとき HIGH
  pinMode(LED, OUTPUT);
}

void loop() {
  if (digitalRead(SW) == LOW) {   // 押された！
    digitalWrite(LED, HIGH);
  } else {
    digitalWrite(LED, LOW);
  }
}
```

- ⚠ `INPUT_PULLUP` は「押すと LOW」。ここは必ずつまずくので、図＋ヒント吹き出しで
- **タクトスイッチ TVGP01-G73BB**（12×12×7.3mm・大型）のデータシートで確認（2026-09-20）
  - **CONTACT ARRANGEMENT: 1 poles 1 throws（1接点1回路）** … 端子は4本だが**スイッチは1つ**。
    同じ側にならぶ2本が常時短絡し、押すと上下がつながる
  - ブレッドボードでは **g行の2本・d行の2本**がそれぞれ常時つながる（実物で確認）
  - **Bounce（チャタリング）: 10ms 以下** … 1回押すと最大10msのあいだ ON/OFF が暴れる。
    「押すたびにON/OFF反転」を作るときは、この対策が要る（b2 の 💪 に明記した）
  - 動作力 180±50gf ／ ストローク 0.25±0.10mm ／ 最大定格 DC12V 50mA ／ 寿命20万回
- 💪 押している間だけでなく「押すたびに ON/OFF が入れかわる」ようにしてみよう（`c3_if` の予習）

### b3 LEDの明るさを変えよう

```cpp
const int LED = 5;   // PWMが使えるピン（D5 = PC3）

void setup() {
  pinMode(LED, OUTPUT);
  analogWriteResolution(8);   // 明るさを 0〜255 で指定する
}

void loop() {
  analogWrite(LED, 0);    delay(500);   // 消灯
  analogWrite(LED, 64);   delay(500);   // ちょっと明るい
  analogWrite(LED, 255);  delay(500);   // いちばん明るい
}
```

- 「本当はものすごく速く点滅している（PWM）」を、手をふって残像で見せる小ネタ
- ⚠ **`analogWriteResolution(8)` は必須**。このボードの既定は 12bit（0〜4095）なので、
  この1行がないと `analogWrite(LED, 255)` はほとんど光らない（→ §6-10）
- ⚠ PWM が使えるのは **0・2・5・6・12 の5本だけ**。→ §9 のピン表

### b4 つまみで明るさを変えよう

```cpp
const int VR  = A2;   // 可変抵抗（つまみ）… A2 = PC4
const int LED = 5;

void setup() {
  pinMode(LED, OUTPUT);
  analogWriteResolution(8);
}

void loop() {
  int value = analogRead(VR);        // 0〜1023（10bit）
  analogWrite(LED, value / 4);       // 0〜255 に変える
  delay(10);
}
```

- ⚠ **シリアルモニタが使えない**ので、`analogRead` の値は「LEDの明るさ」で体感させる設計にする
- 💪 `value / 4` を `map(value, 0, 1023, 0, 255)` に書きかえてみよう

### c2 くりかえし（for）

```cpp
const int LED = 5;

void setup() {
  pinMode(LED, OUTPUT);
  analogWriteResolution(8);
}

void loop() {
  for (int v = 0; v <= 255; v++) {   // だんだん明るく
    analogWrite(LED, v);
    delay(5);
  }
  for (int v = 255; v >= 0; v--) {   // だんだん暗く
    analogWrite(LED, v);
    delay(5);
  }
}
```

- 「同じ命令を255回書くのは大変 → for でまとめる」という**必然性から入る**

### c4 配列と関数でまとめよう

```cpp
const int leds[] = {5, 7, 8};   // 3個のLED
const int N = 3;

void setup() {
  for (int i = 0; i < N; i++) {
    pinMode(leds[i], OUTPUT);
  }
}

void loop() {
  for (int i = 0; i < N; i++) {   // 順番に流れる
    digitalWrite(leds[i], HIGH);
    delay(150);
    digitalWrite(leds[i], LOW);
  }
}
```

### e1 光センサーで暗くなったら光る

```cpp
const int CDS = A2;   // 光センサー（CdS + 10kΩ の分圧）
const int LED = 5;

void setup() {
  pinMode(LED, OUTPUT);
}

void loop() {
  int light = analogRead(CDS);
  if (light < 300) {              // 暗くなったら
    digitalWrite(LED, HIGH);
  } else {
    digitalWrite(LED, LOW);
  }
  delay(50);
}
```

- しきい値 `300` は**教室の明るさで変わる**。「手でかぶせて値を探す」作業そのものを体験にする
- ⚠ シリアルモニタが無いので、値さがしは「LEDが切り替わる境目を数字を変えて探す」方式で

### e3 ものとの距離をはかろう

```cpp
const int TRIG = 9;
const int ECHO = 7;
const int LED  = 8;

void setup() {
  pinMode(TRIG, OUTPUT);
  pinMode(ECHO, INPUT);
  pinMode(LED, OUTPUT);
}

int kyori() {                    // きょりを cm ではかる
  digitalWrite(TRIG, HIGH); delayMicroseconds(10); digitalWrite(TRIG, LOW);
  int w = 0;
  while (digitalRead(ECHO) == LOW) { if (++w > 600) return 999; }
  int n = 0;
  while (digitalRead(ECHO) == HIGH && n < 600) {
    delayMicroseconds(50);       // 音が 1cm 往復する時間
    n++;
  }
  return n - 2;                  // 数えた回数 ＝ ほぼ cm
}

void loop() {
  digitalWrite(LED, kyori() < 20 ? HIGH : LOW);
  delay(100);
}
```

- 「音のはね返る時間 → 距離」という**物理と算数のつながり**を見せられる章
- ⚠ **`pulseIn()` は使えない**（`micros()` の分解能が 1ms しかないため／2026-09-01 実測）。
  かわりに `delayMicroseconds()` で**回数を数える**方式にした
- ✅ **実機で校正ずみ**（2026-09-01）：`delayMicroseconds(50)` で 1回 ≒ 1cm。
  10/20/30/40cm の実測が 12/22/33/43 → **`cm = n - 2` で誤差 ±1cm**
- 説明のしかた：「1回数えるあいだに、音がちょうど 1cm 往復する。
  最初の2回はセンサーが返事をするまでのぶんだから引く」
- `999` を返すのは「反応なし・遠すぎ」。センサー未接続でも固まらない

### e4 サーボモーターを動かそう

```cpp
const int SERVO = 5;

void servoPulse(int us) {        // パルスを1回おくる
  digitalWrite(SERVO, HIGH);
  delayMicroseconds(us);         // 600〜2400 で角度がきまる
  digitalWrite(SERVO, LOW);
  delay(20);                     // 20msごと ＝ 1秒に50回
}

void muku(int kakudo) {          // その角度をむく
  int us = map(kakudo, 0, 180, 600, 2400);
  for (int i = 0; i < 50; i++) servoPulse(us);
}
```

```cpp
void setup() { pinMode(SERVO, OUTPUT); }

void loop() {
  muku(0);    muku(90);
  muku(180);  muku(90);
}
```

- ✅ **実機で確認ずみ**（2026-09-01）。`Servo` ライブラリは**不要**
- 「**20msごとに1.0〜2.0msのパルスを送ると角度が決まる**」という仕組みが
  そのままコードに出るので、ライブラリで隠すより理解しやすい
- ⚠ **1000〜2000us は約90度しか動かない**（これが元々のサーボ規格）。
  180度動かすには **600〜2400us** に広げる。実測で約180度を確認
- ⚠ 可動域はサーボの個体差が大きい。端で「ジー」とうなるときは範囲を狭める。
  **この調整作業自体を教材の題材にする**
- ⚠ サーボは電流を食うので、動いた瞬間にボードが再起動するようなら**外部電源**にする

### e5 モータードライバでDCモーター

```cpp
const int AIN1 = 12;  // どちらも PWM が使えるピンを選ぶ
const int AIN2 = 0;   // （PWM 可は 0・2・5・6・12 の5本だけ）

void setup() {
  pinMode(AIN1, OUTPUT);
  pinMode(AIN2, OUTPUT);
  analogWriteResolution(8);
}

void loop() {
  analogWrite(AIN1, 200);  digitalWrite(AIN2, LOW);   // 正転
  delay(2000);
  digitalWrite(AIN1, LOW); analogWrite(AIN2, 200);    // 逆転
  delay(2000);
  digitalWrite(AIN1, LOW); digitalWrite(AIN2, LOW);   // 停止
  delay(1000);
}
```

- 「なぜマイコンから直接モーターをつないではいけないか（電流・逆起電力）」を1スライドで

---

## 3. 学習の流れ（何が積み上がるか）

```
【導入】          【基本】                      【制御】              【発展】
i3 Lチカ ─→ b1 外付けLED ─→ b3 PWM ────┐
   │                                    ├─→ c2 for ────┐      ┌─→ e1〜e3 センサー ─┐
   └─→ b2 スイッチ(digitalRead) ──┐     │              ├─ c4 ─┤                    ├─→ e6 作品
                                  ├─→ c3 if ───────────┘ 配列  └─→ e4〜e5 モーター ─┘
       b4 可変抵抗(analogRead) ────┘                     関数
```

**「出力 → 入力 → 入力で出力を変える → くりかえし・条件で自動化 → 実世界のデバイス」** という一本道にしてあります。
発展編は `analogRead` の延長にある**センサー編（e1〜e3）を先に**、`analogWrite` の延長にある
**モーター編（e4〜e5）をあとに**置き、最後に両方を掛け合わせて作品にします。

---

## 4. コース設計（半日 → 1日 → 複数日）

**教材は共通で、進む章の範囲だけを変えます。** どのコースも「導入編で全員がLチカを成功させる」ところから始まります。

| コース | 時間 | 進む範囲 | ゴール（参加者が言えること） |
|---|---|---|---|
| **A. 半日（体験）** | 3時間 | 導入編 ＋ 基本編 b1〜b4 | 「LEDをつけて、スイッチや つまみ で操作できた」 |
| **B. 1日** | 6時間 | ＋ 基本編 b5 ＋ 制御編 c1〜c4 | 「くりかえしと条件を自分で書けた」 |
| **C. 複数日（全4回）** | 3時間×4 | ＋ 発展編 e1〜e6（センサー→モーター→作品） | 「センサーで感じて、モーターで動く作品を作った」 |

### A. 半日コース（3時間）— まずはここから

| 時刻 | 内容 |
|---|---|
| 0:00–0:15 | i1 マイコンと UIAPduino ってなに？ |
| 0:15–0:45 | i2 Arduino IDE のじゅんび（PC準備済みなら 15分に短縮可） |
| 0:45–1:15 | i3 はじめてのLチカ ★**ここで全員成功させる**（このコースの山場） |
| 1:15–1:25 | 休憩 |
| 1:25–1:50 | b1 ブレッドボードとLED |
| 1:50–2:15 | b2 スイッチでLEDをつけよう |
| 2:15–2:45 | b3・b4 明るさを変えよう（PWM／可変抵抗） |
| 2:45–3:00 | まとめ・かたづけ・持ち帰り案内（続きは家でもできる、と伝える） |

### B. 1日コース（6時間）

| 時刻 | 内容 |
|---|---|
| 午前 0:00–0:45 | i1・i2 導入とセットアップ |
| 午前 0:45–1:15 | i3 Lチカ |
| 午前 1:25–2:40 | b1〜b4 入出力（休憩含む） |
| **昼休み** | |
| 午後 0:00–0:20 | b5 ブザーで音を鳴らそう |
| 午後 0:20–1:10 | c1・c2 変数と for |
| 午後 1:20–2:15 | c3・c4 if と 配列・関数 |
| 午後 2:15–3:00 | ミニ作品（ここまでの部品だけで自由制作）＋ 発表 |

### C. 複数日コース（3時間×4回）

| 回 | 範囲 | その日の成果物 |
|---|---|---|
| 第1回 | 導入編 ＋ 基本編 b1〜b4 | スイッチとつまみで光るLED（＝半日コースと同じ） |
| 第2回 | b5 ＋ 制御編 c1〜c4 | 流れるLED（ナイトライダー） |
| 第3回 | 発展編 e1〜e3（**センサー**） | 暗くなったら光る自動ライト／距離で反応する装置 |
| 第4回 | 発展編 e4〜e6（**モーター**＋作品） | センサーで動くオリジナル作品 → 発表会 |

> 各回のはじめに前回の復習を10分入れる想定。第2回以降は書き込み手順に慣れているぶん、進みが速くなります。

---

## 5. ライブラリの方針（＝**全23章でいっさい不要**）

> 以下は **インストール済みのボードパッケージ（UIAP / ch32v 1.0.42）のソースを直接確認した結果**であり、推測ではありません。

### 同梱ライブラリ（インストール不要・`#include` するだけ）

`Adafruit_TinyUSB_Arduino` / `EEPROM` / `SPI` / `USBPD_SINK` / `Wire`

### コアに実装済み（`#include` すら不要）

`digitalWrite` `digitalRead` `pinMode(INPUT_PULLUP)` `analogRead` `analogWrite`
`analogReadResolution` `analogWriteResolution` `analogWriteFrequency`
**`tone` / `noTone`** `delay` `millis` `map` `random` `shiftOut` `delayMicroseconds`
（⚠ `micros()` は 1ms 刻み・`pulseIn()` は 1ms 未満を測れないため**使わない**）

### 章ごとの要否

| 章 | ライブラリ | 備考 |
|---|---|---|
| 導入編 i1〜i3 | **不要** | |
| 基本編 b1〜b4 | **不要** | |
| 基本編 b5 ブザー | **不要** | `tone()` はコアに実装済み（`Tone.cpp` を確認） |
| 制御編 c1〜c4 | **不要** | `map()` もコアに実装済み |
| 発展編 e1 光センサー | **不要** | CdS は分圧して `analogRead` するだけ |
| 発展編 e2 温度センサー | **部品選びしだい** | **MCP9701／LM35 などアナログ式を選べば不要**。DHT11・DS18B20 はライブラリ必須なので**採用しない** |
| 発展編 e3 距離センサー | **不要** | `digitalRead` ＋ `delayMicroseconds` で自作（`pulseIn` は使えない） |
| 発展編 e4 サーボ | **不要** ✅ | `Servo.h` は同梱されていないが、`digitalWrite` ＋ `delayMicroseconds` でパルスを自作して実機動作を確認（2026-09-01） |
| 発展編 e5 モーター | **不要** | `digitalWrite` と `analogWrite` だけで足りる |

### 結論：ライブラリの説明は付録に逃がす

**半日コース・1日コースは、ライブラリの話がいっさい出てきません。**
初登場は発展編の e4（サーボ）＝複数日コースの第4回だけ。したがって、

- ライブラリ導入の解説は付録 `x5_library` に置き、**本編からはリンクするだけ**にする
- 部品はできるだけ「ライブラリの要らないもの」を選ぶ（温度センサーはアナログ式、距離センサーは自作の計測ループ）
- e4 サーボは次の2案。**A案を第一候補**とし、`x5_library` はあくまで「自由制作で使いたい人むけ」に留める

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| **A. ライブラリを使わない** | `analogWriteFrequency(50)` ＋ `analogWrite()` でサーボ信号を自作 | 追加インストール不要。PWMの復習になる | 角度→duty の計算を教える必要がある |
| B. Servo ライブラリを入れる | ライブラリマネージャで CH32 対応の Servo を導入 | コードが `servo.write(90)` で済む | 導入手順が要る／CH32V003 対応品の見きわめが必要／16KBを圧迫 |

> ⚠ **Flash 16KB / RAM 2KB** なので、そもそも一般的なライブラリの多くは入りません。
> 「ライブラリを使わない」のは教育上の都合だけでなく、**このボードの現実的な制約**でもあります。

---

## 6. UIAPduino ならではの注意点（教材づくりで必ず押さえる）

| # | 内容 | 教材への反映 |
|---|---|---|
| 1 | **書き込みが独特**：RESET押しながらUSB挿す → 書き込み → 書き込み後もう一度RESET | `i3` に写真つきで大きく。`x2_trouble` にも再掲。当日はメンターが全員分を1回は一緒にやる |
| 2 | **シリアルモニタが使えない**（USB CDC非対応／UARTはPD5に外付け変換が必要） | `Serial.print` デバッグを前提にしない。**値はLEDの明るさ・点滅回数で見せる**設計に。「Arduinoの本に出てくるシリアルモニタは、このボードでは使えないよ」と明記 |
| 3 | **ピンヘッダは未実装**（別売・要はんだ付け） | **当日は はんだ付け済みボードを配布**。自分で買った人むけに付録 `x4_solder` で手順を用意し、教材ページからいつでも見られるようにする |
| 4 | **Flash 16KB / RAM 2KB** | 大きなライブラリ（重いOLED、NeoPixel多数など）は載らない可能性。サンプルは小さく保つ |
| 5 | **11（PD1）は SWIO（デバッグ用）と共用** | 使うには `setup()` に `pinV32_DisconnectDebug(PD_1);` が必要（SWIOは無効化）。教材では**使わない**方針。ピン配置図で「使わない」と表示 |
| 6 | **ピン7・8・9は左右で共通** | 配線図で誤解が出やすいので図に注記 |
| 7 | **3.3V / 5V 切替**（ジャンパ） | 5V固定で配布し、「触らない」と明記するのが安全 |
| 8 | **長いUSBケーブルで書き込み失敗**することがある | 短めの良品ケーブルを主催側で用意 |
| 9 | macOS はソフトウェアUSB由来の相性問題の報告あり | 参加者PCは **Windows推奨**と案内。Mac参加者は事前検証 |
| 10 | **`analogWrite` の既定が 12bit（0〜4095）** ⚠ | Arduino標準の 0〜255 と違う。`setup()` に **`analogWriteResolution(8);` を必ず入れる**。入れないと `analogWrite(pin, 255)` はほぼ光らない。教材では「明るさを0〜255で指定する、と決める1行」として説明する |
| 11 | **`LED_BUILTIN` が定義されていない** | `const int LED = 2;` と自分で書く。教材の1行目から効いてくる |
| 12 | **PWM が使えるピンが少ない** | 公式図で PWM 表示があるのは **0・2・5・6・12 の5本だけ**。**3・4・7・8・9・10・15・16 は不可**（§9 のピン表） |
| 13 | `tone()` はタイマー TIM2 を使う | ✅ **実測で干渉なし**（2026-09-01）。`tone()` を鳴らしながら D2 の `analogWrite` がなめらかにフェードした。**音とLEDは同時に使える** |
| 14 | `analogRead` は 10bit（0〜1023） | Arduino標準どおり。教材の `value / 4` はそのまま使える |

---

## 7. 部品リスト（1人分・目安）

| 部品 | 数 | 目安価格 | 備考 |
|---|---|---|---|
| UIAPduino Pro Micro CH32V003 V1.4 | 1 | ¥290 | **主催側でピンヘッダをはんだ付けして配布**（付録 `x4_solder` に手順） |
| ピンヘッダ（2.54mm・12ピン×2） | 1組 | ¥30 | 上記に使用 |
| USB Type-C ケーブル（データ通信可・短め） | 1 | ¥300 | 充電専用ケーブルは不可 ⚠ |
| ブレッドボード（ハーフ） | 1 | ¥200 | |
| ジャンパーワイヤ オス-オス | 10本 | ¥200 | |
| LED（赤・黄・緑） | 3 | ¥60 | |
| 抵抗 1kΩ | 3 | ¥30 | LED用（330〜1kΩ で代用可。330Ω だと高輝度LEDはまぶしい） |
| 抵抗 10kΩ | 2 | ¥20 | CdS分圧・プルアップ予備 |
| タクトスイッチ | 2 | ¥40 | |
| 半固定抵抗 10kΩ | 1 | ¥60 | |
| 圧電ブザー | 1 | ¥60 | b5 用 |
| CdS（光センサー） | 1 | ¥50 | e3 用 |
| **小計（持ち帰りキット）** | | **約 ¥1,300** | |
| サーボ SG90 | 1 | ¥500 | 発展・グループ共用可 |
| モータードライバ + DCモーター | 1 | ¥500 | 発展・グループ共用可 |
| 距離センサー HC-SR04 など | 1 | ¥300 | 発展・グループ共用可 |

主催側で別途：予備ボード（数枚）、USBハブ、テスター、ニッパー、部品トレイ。

---

## 8. 雛形（microbit リポジトリ）からの変更点

| 対象 | 変更内容 |
|---|---|
| `build.py` の `SECTIONS` | 「基礎編／拡張編／ロボットカー編／コーディング編」→「**導入編／基本編／制御編／発展編／付録**」の5つに差し替え |
| もくじページ | どこまで進むかでコースが変わるので、**セクション見出しに「半日はここまで」「1日はここまで」の目印**を出せると親切（`SECTIONS` の `sub` に書くだけでも可） |
| `docs/assets/deck.js` | コードの色分けが **js / py のみ対応**（`LANG_LABEL` `KEYWORDS` `langKey`）。**`cpp` / `ino`（Arduino）を追加する必要あり** ← 最初にやる改修 |
| コードブロックの分量 | Arduino版は**ほぼ全スライドにコードが載る**。「1スライド14行まで」の制約が効いてくるので、コードは細かく分割して提示 |
| `_template/index.md` | 「説明＋画像」の2カラムに加えて「**説明＋コード**」「**回路図＋コード**」のパターンを追加 |
| 画像 | micro:bit 版の画面キャプチャ中心 → **配線写真・ブレッドボード図（Fritzing など）** が主役になる |
| README / タイトル | 「UIAPduino 体験ワークショップ」に差し替え |

---

## 9. 実機検証プラン（ボードは手元にあるので随時実施）

**執筆順と同じ順に検証していく**のが効率的です。第1段階が通れば半日コース（＝最優先）の教材が書けます。

> 📁 第1段階の**検証用スケッチ一式は [`verify/`](verify/) にあります**（v01〜v06）。
> 使いかたと結果の記入欄は [`verify/README.md`](verify/README.md) を参照。

### ピン配置（**公式ピン配置図**が出典・2026-08-31 訂正）

出典：UIAPduino Pro Micro CH32V003 公式ピン配置図（Yuuki U. 作／**CC BY 4.0**）。
画像は `content/x3_pinout/image/index/pinout.png` に置いてあります。

> ⚠ 以前このセクションに載せていた表は、ボードパッケージの `variant_CH32V003F4.h` から
> **推測で作ったもので、誤りがありました**（下の「訂正した点」参照）。**公式図が正**です。

| Arduino | MCU | アナログ | PWM | 備考 |
|---|---|---|---|---|
| 0 | PA1 | A1 | ✓ | |
| 1 | PA2 | A0 | – | ✅実測：`digitalWrite` 可・`analogWrite` は**無反応** |
| 2 | PC0 | – | ✓ | **基板のオレンジLED** |
| 3 | PC1 | – | – | I2C SDA・基板に 2.2kΩ プルアップあり |
| 4 | PC2 | – | – | I2C SCL・基板に 2.2kΩ プルアップあり |
| 5 | PC3 | – | ✓ | ✅実測：PWM 可 |
| 6 | PC4 | A2 | ✓ | SPI SS ／ ✅実測：PWM 可 |
| 7 | PC5 | – | – | SPI SCK（左右どちらの列にもある） |
| 8 | PC6 | – | – | SPI MOSI（左右どちらの列にもある） |
| 9 | PC7 | – | – | SPI MISO（左右どちらの列にもある） |
| 10 | PD0 | – | – | ✅実測：`digitalWrite` 可・`analogWrite` は**無反応** |
| 11 | PD1 | – | – | ⚠ SWIO（デバッグ用）と共用。使うには `pinV32_DisconnectDebug(PD_1);` が必要・教材では**使わない** |
| 12 | PD2 | A3 | ✓ | ✅実測：PWM 可 |
| 15 | PD5 | A5 | – | UART TX ／ ✅実測：アナログ入力 可 |
| 16 | PD6 | ❌ | – | UART RX ／ ✅実測：**`analogRead` は使えない**（コアの PinMap_ADC の記述ミス） |

**ピンとして出ていないもの**

- `PD7` … **RESET**（ボタンとして使われている。GPIO ではない）
- `PD3` / `PD4` … **USB の D+ / D−**（USB通信に使われている。**A4・A7 は存在しない**）
- 番号 **13・14・17 は欠番**

その他：

### 設計判断：`analogWrite` は 0〜255 で統一する（2026-09-03 決定）

`setup()` に `analogWriteResolution(8);` を入れて **0〜255** で書く方針とした。
既定の 0〜4095 のまま使う案も検討したが、次の理由で 0〜255 を採用。

- **外部の Arduino 記事・書籍がすべて 0〜255 前提**。i1 で「作例がネットにたくさんある」と
  伝えている以上、調べたコードがそのまま動く状態にしておきたい
- **c2 のフェードが素直に書ける**。0〜4095 だと `v++` で4096回まわるため片道20秒になり、
  `v += 16` か `delay(1)` の追加が必要。`for` の基本形を教える章が濁る
- 数が小さく扱いやすい（255・128・64）

0〜4095 側の利点（`analogRead(0〜1023)` → `× 4` で対応でき、`÷ 4` より易しい）は認めるが、
章によって範囲が変わるのは混乱のもとなので統一を優先した。

**精度は落ちない**：`wiring_analog.c` を確認したところ、`analogWrite` は値を内部で
12bit に引き伸ばしてからタイマーに設定している（`mapResolution` → `pwm_start`）。
0〜255 にしてもハードウェアの動作は 12bit のままで、指定できる段階が 256 になるだけ。

---

- **動作電圧**：**既定 5V**（3.3V にも選択可・出力ピンの電圧は選択と無関係）
  → **ADC 基準電圧も 5V**。電圧換算は `v * 5.0 / 1024`
- **5Vピンから取り出せる電流：連続で約 0.5A まで**（2026-09-20 公式ページで確認）
  5Vピンは USB の VBUS と**レギュレータを介さず**つながっているが、間に**オンボードヒューズ
  （F1 / 06H1100D）**が入っている。公式の記載：

  > USB Type-CのVBUSと5V出力ピンはオンボードヒューズを介してボードの内部で電気的に接続されています。
  >
  > VBUSのオンボードヒューズの溶断電流は1Aです。一般的にはその半分がヒューズの連続定格電流です。

  **1A は「流せる上限」ではなくヒューズが切れる値**。実用上の上限は **約 500mA** と考える。
  さらに上流の USB ホスト側にも制限がある（USB 2.0 標準ポートは 500mA）。
  このヒューズはリセッタブルではないので、**切れたら基板交換**になる。

  部品ごとの目安：HC-SR04 約15mA・MCP9701／CdS 数mA は問題なし。
  **SG90 サーボは無負荷 100〜250mA だが、ストール時に約 700mA** まで上がるため連続定格を超える。
  DCモーターは数百mA〜1A で論外。**e4・e5 で外部電源を案内しているのは、この理由**。
- **タイマーの割り当て**（`variant_CH32V003F4.h` / `PeripheralPins.c` で確認・2026-09-20）
  `TIMER_TONE = TIM2` ／ `TIMER_SERVO = TIM1`。PWM は次のとおり：

  | ピン | 端子 | タイマー | PWM |
  |---|---|---|---|
  | 0/A1 | PA_1 | TIM1_CH2 | ✅ |
  | 1/A0 | PA_2 | TIM1_CH2**N** | ❌ 相補出力 |
  | 2 | PC_0 | **TIM2**_CH3 | ✅ |
  | 5 | PC_3 | TIM1_CH3 | ✅ |
  | 6/A2 | PC_4 | TIM1_CH4 | ✅ |
  | 10 | PD_0 | TIM1_CH1**N** | ❌ 相補出力 |
  | 12/A3 | PD_2 | TIM1_CH1 | ✅ |

  → **実測結果（PWM 可は 0・2・5・6・12、1 と 10 は不可）がコードで完全に裏づけられた**。
  1 と 10 が効かないのは**相補出力チャンネル（CH1N・CH2N）**で、`pwm_start` が
  通常チャンネルしか扱わないため。3・4・7・8・9・15・16 は `PinMap_TIM` に無いので元から PWM 不可。

  ⚠ **`tone()` は TIM2 を使う**ので、**2番ピンの `analogWrite` と干渉する**（後から呼んだほうが周期を上書き）。
  ⚠ `tone()` は**同時に1ピンだけ**。別ピンで呼ぶと**黙って無視される**。
  `noTone(pin)` は既定が `destruct=false` でタイマーを止めるだけなので、
  別ピンに移すには **`noTone(pin, true)`** が必要。
- **分解能**：`analogRead` = **10bit（0〜1023）** ／ `analogWrite` = **12bit（0〜4095）**
- **PWM 周波数**：既定 **1000Hz**（`pins_arduino.h` の `PWM_FREQUENCY`）。
  48MHz ÷ 分周1 ÷ 周期48000 でちょうど 1000Hz を作れるが、**システムクロック自体に約1.7%の誤差**
  があるため実際は 1000±20Hz 程度。**PWM 周波数そのものは未実測**なので、教材では「約1000回」と書く
- **`LED_BUILTIN` は未定義** → 自分で `2` と書く
- 教材で使うピン（確定）：**2**=内蔵LED ／ **5・6**=LED（PWM可）／ **7・8・9**=LED（点滅のみ）／
  **3**=スイッチ ／ **A2（=6）**=アナログ入力 ／ **A1（=0）・A3（=12）**=センサー増設用

### 訂正した点（推測 → 公式図）

| 項目 | 誤（推測） | 正（公式図） |
|---|---|---|
| ピン 0・1・6 | 存在しない | **存在する**（0=PA1、1=PA2、6=PC4） |
| ピン 17 | PD7 として使える | **欠番**。PD7 は RESET |
| A4・A7 | 使える（PD3・PD4） | **存在しない**。PD3・PD4 は USB の D+/D− |
| A5・A6 | A6 は要確認 | **どちらも存在する**（15=PD5、16=PD6） |
| PWM 可のピン | 2・5・10・17・A0〜A4・A7 | **0・2・5・6・12 の5本**（10・11 は公式図で PWM 表示なし） |

> `PeripheralPins.c` の `PinMap_TIM` には PD0(10)・PA2(1)・PD1(11) も載っていますが、
> これらは **TIM1 の相補出力（CH1N/CH2N/CH3N）** で、公式図では PWM として案内されていません。
> `analogWrite` が効くかどうかは **v04 で実測**して判断します。

### 第1段階：半日コースの土台（`i`・`b1`〜`b4`）

- [x] **ピン配置は公式図で確定**（§9 のピン表・`x3_pinout` に画像を掲載済み）
- [x] **1・10 の `analogWrite`** … ❌ **無反応**（2026-09-01 実測）。相補出力のため MOE が立たず端子に出ない。
  `digitalWrite` なら使える。**PWM 可は公式図どおり 0・2・5・6・12 のみ**と確定
- [x] **A5・A6 の `analogRead`** … A5 ✅可 ／ **A6 ❌不可**（2026-09-01 実測）。
  コアの `PinMap_ADC` が存在しない `PA_6` を登録しているための不具合。
  **使えるアナログ入力は A0・A1・A2・A3・A5 の5本**
- [ ] `analogWriteResolution(8)` を入れた場合／入れない場合の見え方の違いを確認（教材の説明文に使う）
- [x] `INPUT_PULLUP` の動作確認 … ✅ **3・4 とも期待どおり**（2026-09-01）。
  基板の 2.2kΩ プルアップは支障なし。常時LOWのピンも無し → **b2 のスイッチは 3番で確定**
- [x] 書き込み手順の再現と、**書きこみモードを忘れたときのエラー**を採取（2026-08-31）
  → `Error: Could not initialize any supported programmers` ／
  `Failed uploading: uploading error: exit status 0xfffffe0`
  （i3 に「⚠ こんなエラーが出たら」スライドとして掲載済み。`x2_trouble` でも再掲する）
- [ ] ほかの**失敗するパターン**（長いケーブル・USBハブ・RESETを離すタイミング）でエラー文が変わるか確認
- [ ] Arduino IDE 2.x のバージョンを記録（ボードパッケージは **UIAP ch32v 1.0.42** を確認済み）＋ セットアップ画面のキャプチャ取得

### 第2段階：1日コース（`b5`・`c`）

- [x] `tone()` / `noTone()` はコアに実装済み（`Tone.cpp` を確認）→ **b5 は実施可能**
- [x] `tone()` … ✅ **使用可・どのピンでも鳴る**（GPIOトグル実装）。周波数誤差 +0.8%。
  TIM2 を使う D2 の PWM との**干渉もなし**（2026-09-01 実測）→ ブザーは **7番**に置く
- [ ] `const int leds[]` ＋ `for` の配列サンプルが期待どおり動くか
- [x] Flash/RAM の使用量 … **Lチカで 6988/16384（42%）・536/2048（26%）**（2026-08-31 実測）
- [ ] ⚠ **残り Flash は 9KB ほど**しかない。発展編（特に e3 距離センサー・e5 モーター）のサンプルが収まるか要確認

### 未検証（部品が手元にないため）

- [x] **e1 光センサー（CdS）** … **暗抵抗 1MΩ**（GL5528 相当）に決定。10kΩ と組にする。
  値の向きは回路から確定：`5V ── CdS ── A2 ── 10kΩ ── GND` なので **明るいほど値が大きい**
  （明るい＝CdSの抵抗が小さい＝A2 が 5V 側に引かれる）
  - 想定値：室内 約680／手でおおって 約170 → しきい値 300 付近が妥当
  - [ ] 実機で振れ幅を確認。足りなければ **固定抵抗のほう**を変える（CdS ではなく）
- [x] **e2 温度センサー** … ✅ **MCP9701-E/TO に確定**（2026-09-23・実機で判明）
      ⚠ **以前ここに「MCP9700 に確定」と書いていたが、手元の実物は 9701 だった。**
      | 品種 | 0℃の出力 | 1℃あたり |
      |---|---|---|
      | MCP9700/9700A/9700B | 500 mV | 10.0 mV/℃ |
      | **MCP9701/9701A** | **400 mV** | **19.5 mV/℃** |
      （Microchip DS20001942L。TO-92 の足の並びは 9700 と同じ）
      **ボードの動作電圧は既定 5V** なので、ミリボルトに直して
      `(v * 5000.0 / 1024 - 400) / 19.5` が℃。3.3V動作なら `5000`→`3300`。
      ※ 実物での校正（室温の温度計と見くらべ）は当日または事前に一度行う
      ✅ **TO-92 の足の並びを確認**（Microchip DS20001942L の Package Types）。
      3-Pin TO-92 は **1=VDD / 2=VOUT / 3=GND**。平らな面を手前に見て左から
      **VDD・VOUT・GND**。教材の記述と一致していた。
      絶対最大定格 **VDD 6.0V**／出力電流 ±30mA、0℃で 500mV も確認ずみ。
      - [ ] ⚠ 「逆につなぐと熱くなる」は**資料に記載がない**。注意書きとしては
            安全側だが、断定はしていない。実機で確かめるまでこのまま。
- [x] **e5 DRV8835モジュール**（秋月 109848）… ✅ **VCC は 5V に確定**（資料で確認）
      モジュール資料 AE-DRV8835-S の仕様表に **入力信号 VIN の範囲は「0〜Vcc」**、
      絶対最大定格は **「−0.5〜Vcc+0.5V」** とある。
      → VCC を 3V3 にしたまま 5V のマイコンから合図を入れると **定格超過**。
      VCC はロジックの基準電圧なので、**ボードと同じ 5V** につなぐのが正しい。
      （ロジック電源 2〜7V なので 5V は範囲内。モータ電源 2〜11V・1回路1.5A）
      教材は 3V3 と書いていたので修正した（`e5_motor` と `bb_drv8835.png`）。
      <https://akizukidenshi.com/goodsaffix/AE-DRV8835-S_20210526.pdf>
      - [x] ✅ **端子の並びを資料で確定**（2026-09-23）。ピン番号は
            `1 VM / 2 AOUT1 / 3 AOUT2 / 4 BOUT1 / 5 BOUT2 / 6 GND /
             7 BIN2 / 8 BIN1 / 9 AIN2 / 10 AIN1 / 11 MODE / 12 VCC`。
            12ピンDIP なので **1番の向かいが 12番**。横向きにさすと
            上の行 `VCC MODE AIN1 AIN2 BIN1 BIN2`／
            下の行 `VM AOUT1 AOUT2 BOUT1 BOUT2 GND` になる。
            ⚠ **図はこれと上下が入れかわった鏡像だった**ので描き直した。
            実物写真とも一致を確認（写真の左列＝7〜12、右列＝6〜1）。
            端子だけの拡大図を `drv8835_pins.png` として1枚用意した
            （作図は `tools/fig_drv8835_pins.py`）。**配線図と同じ向き・
            同じ色**にして、見くらべたときに迷わないようにしてある。
      - [x] ~~`MODE`=GND で IN/IN になることは実機で確認する~~ → PHASE/ENABLE（MODE=＋）に
            変えたので不要。**MODE=＋ の PHASE/ENABLE で正転・逆転を実機確認**（2026-09-24）

### 第3段階：発展編（`e`）

- [ ] CdS の分圧回路で `analogRead` が想定レンジ（教室の明暗で有意差）になるか → しきい値の目安を決める
- [ ] **人感センサー SB412A**（秋月 109002）… 出力の HIGH が **3V 固定**なので、
  5V 動作のこのボードの `digitalRead` で読めるかを実機で確かめる（`verify/v11_pir/`）。
  電源 3.5〜12V・待機 20µA・検知保持 2〜4秒・5m・115°。**章を書くのはこの結果が出てから**
- [x] 温度センサーの品種決定 … ✅ **MCP9701**。換算式も確定（上記）
- [x] `pulseIn()` … 実装はあるが ❌ **実用にならない**（2026-09-01 実測）。
  `micros()` の分解能が 1ms しかなく、1ms 未満のパルスで 0 を返す。
  **迂回策**：`digitalRead()` ＋ `delayMicroseconds()` で回数を数える → 実機で動作確認ずみ
- [x] サーボ … ✅ **ライブラリ不要で動作確認**（2026-09-01）。`digitalWrite` ＋ `delayMicroseconds` で
  20msごとに 600〜2400us のパルスを送る方式。約180度の可動を実測
- [x] `analogWriteFrequency()` … ✅ **効く**（v10a）。ハードウェアPWMでもサーボは駆動できるが、
  仕組みが見える自作パルス方式を教材に採用した
- [ ] `analogWriteFrequency(50)` ＋ `analogWrite` でサーボが動くか（12bit なので duty は 0〜4095 で計算）
- [ ] サーボ／モーターの電源をどう供給するか（USB直結の可否、外部電源の構成）を決める
- [ ] モータードライバの品種決定（DRV8833 / TB6612）と結線の確定

> 検証で分かったことは、この §8 のチェックを埋めるだけでなく **`x3_pinout` と `x2_trouble` にそのまま流用**できます。

---

### 第4段階：拡張編（`m`）

- [x] **I2C が 3番・4番で動くか**／**LCD のアドレス** … ✅ 動く。**アドレスは 0x3F**（2026-09-24）
      v23 を 0x3F にして **Hello! の表示を確認**。0x27（PCF8574T）ではなく 0x3F（PCF8574AT）の基板だった。
      → 章のコードを **0x3F** にし、「出ないときは 0x27」と案内する形にした。
      ⚠ 見た目が同じでも 0x27 の基板が混じることがある。**当日の LCD はぜんぶ v22 で調べておく**
- [x] **「LiquidCrystal I2C」がこのボードでコンパイルできるか**・警告の有無 … `verify/v23_lcd/`
      ✅ 通る（2026-09-24）。「互換性がないかもしれません」の**警告は出る**ので、章に「そのまま書きこめる」と書いた
- [x] **Flash に入るか**。✅ 入る。**14276 / 16384（87%）**・RAM 740 / 2048（36%）
      ⚠ Lチカ（42%）から**約 7KB 増えた**。**残りは約 2KB**。章に「9割近くを使う」と書いた
- [ ] 💪 の組みあわせ（温度の `float` 表示・`map`）が Flash に入るか。
      `lcd.print(float)` は小数を出すぶん大きくなる。⚠ **残り 2KB なので要注意**。
      入らなければ、温度は 10倍した整数で出す（`int t10 = ...;`）などに変える
- [ ] 章の3つのプログラム（`m1_hello`・`m1_count`・`m1_vr`）を実機で通す
      ✅ `m1_count`（P9）… カウントアップの表示を確認・Flash 14524（88%）（2026-09-24）
      ✅ `m1_hello` 相当 … v23 を 0x3F にして Hello! の表示を確認
      ✅ `m1_vr`（P11）… 0〜1023 の表示を確認（2026-09-24）。うしろの桁が残らないかは確認中
      ⚠ **Flash 15780 / 16384（96%）**。`analogRead` を足しただけで約 1.2KB 増え、**残り約 600 バイト**。
      → 💪 が入るかを `verify/v24_lcd_fit/` で確かめた（2026-09-24・コンパイルのみ）
      - map で %：15876（96%）✅ ／ 距離 kyori()：15336（93%）✅
      - **温度を float で表示：❌ 10232 バイトはみ出す**（小数の表示で 10KB 以上ふえる）
      - 温度を整数で（×100÷195 で 0.1℃ 単位）：15908（97%）✅
      → 💪 の前に「⚠ 小数はメモリを食う」のページを足し、整数で出すやり方を見せた。
        エラー文の `overflowed` ＝メモリ不足、も教える
      ⚠ e2 単体は float でも入る（LED で知らせるだけで、小数を**表示**しないため）
- [ ] 確認できたら `wip: 実機確認まち` を外し、章の「⚠ 要確認」を消す

## 10. 参考リンク

- UIAPduino Pro Micro CH32V003 V1.4 公式: https://www.uiap.jp/uiapduino/pro-micro/ch32v003/v1dot4
- スイッチサイエンス: https://www.switch-science.com/products/9914
- ボードマネージャURL: `https://github.com/YuukiUmeta-UIAP/board_manager_files/raw/main/package_uiap.jp_index.json`
- セットアップ解説（ソースに絡まるエスカルゴ）: https://rikoubou.hatenablog.com/entry/2026/03/02/124653
- CH32V003 開発ガイドブック Arduino抜粋版: https://ch32v003-guidebook-arduino.74th.tech/2-arduino/index.html
- fab-wiki: https://scrapbox.io/fab-wiki/UIAPduino_Pro_Micro_CH32V003
